# 🌐 Доступ до сторінок та інтерфейсів TesiGo

> Повний перелік всіх доступних сторінок, адмін-панелі та API ендпоінтів для локального запуску

---

## 📋 Зміст

1. [Frontend сторінки (веб-інтерфейс)](#frontend-сторінки)
2. [Особистий кабінет (Dashboard)](#особистий-кабінет)
3. [Адмін-панель через API](#адмін-панель-через-api)
4. [API документація](#api-документація)
5. [Службові ендпоінти](#службові-ендпоінти)

---

## 🎨 Frontend сторінки

### Базовий URL
```
http://localhost:3000
```

### Доступні сторінки

#### 1. **Головна сторінка**
- **URL:** `http://localhost:3000/`
- **Опис:** Головна сторінка з інформацією про платформу
- **Функціонал:**
  - Hero секція
  - Особливості платформи
  - Як це працює
  - Тарифи
  - Реєстрація/Вхід

#### 2. **Особистий кабінет (Dashboard)**
- **URL:** `http://localhost:3000/dashboard`
- **Доступ:** Потребує авторизації
- **Опис:** Головна панель користувача
- **Функціонал:**
  - Статистика профілю
  - Список документів
  - Остання активність
  - Форма генерації секції

#### 3. **Профіль користувача**
- **URL:** `http://localhost:3000/dashboard/profile`
- **Доступ:** Потребує авторизації
- **Опис:** Особистий кабінет користувача
- **Функціонал:**
  - Інформація про акаунт
  - Email адреса
  - Статус верифікації
  - Дата реєстрації
  - Статистика використання (токени, вартість)

#### 4. **Налаштування**
- **URL:** `http://localhost:3000/dashboard/settings`
- **Доступ:** Потребує авторизації
- **Опис:** Налаштування акаунта
- **Функціонал:**
  - Мова інтерфейсу (Українська/English)
  - Тема (Світла/Темна/Системна)
  - Вихід з акаунта
  - API налаштування (в розробці)

#### 5. **Документи** *(в навігації, але сторінка не реалізована)*
- **URL:** `http://localhost:3000/dashboard/documents`
- **Статус:** ⚠️ Планується

---

## 👤 Особистий кабінет

### Як потрапити

1. **Відкрийте головну сторінку:**
   ```
   http://localhost:3000
   ```

2. **Зареєструйтеся або увійдіть:**
   - Натисніть кнопку "Вхід" або "Реєстрація"
   - Запросіть magic link на ваш email
   - Перейдіть за посиланням у листі

3. **Після авторизації:**
   - Вас автоматично перенаправить в `/dashboard`
   - Або перейдіть вручну: `http://localhost:3000/dashboard`

### Навігація в кабінеті

В лівому меню доступні:
- **Dashboard** → `/dashboard`
- **Documents** → `/dashboard/documents` (в розробці)
- **Профіль** → `/dashboard/profile`
- **Settings** → `/dashboard/settings`

---

## 🔐 Адмін-панель через API

### Важливо
⚠️ **Frontend адмін-панелі НЕ існує.** Адмін-функції доступні тільки через API.

### Передумови
1. **Користувач повинен мати права адміністратора:**
   - В базі даних поле `is_admin = True` для вашого користувача

2. **Потрібен JWT токен:**
   - Отримайте токен через авторизацію
   - Додайте в заголовок: `Authorization: Bearer <your-token>`

### Як зробити користувача адміністратором

#### Варіант 1: Через SQL
```sql
-- Підключіться до PostgreSQL
psql -h localhost -U tesigo_user -d tesigo_db

-- Зробіть користувача адміном (замініть email)
UPDATE users SET is_admin = TRUE WHERE email = 'your-email@example.com';
```

#### Варіант 2: Через Python скрипт
```python
import asyncio
from sqlalchemy import select
from app.core.database import AsyncSessionLocal
from app.models.auth import User

async def make_admin(email: str):
    async with AsyncSessionLocal() as db:
        result = await db.execute(select(User).where(User.email == email))
        user = result.scalar_one_or_none()
        if user:
            user.is_admin = True
            await db.commit()
            print(f"✅ {email} тепер адміністратор")
        else:
            print(f"❌ Користувача {email} не знайдено")

# Запустіть
asyncio.run(make_admin('your-email@example.com'))
```

### Адмін-ендпоінти

Базовий URL: `http://localhost:8000/api/v1/admin`

#### 1. **Статистика платформи**
```bash
GET http://localhost:8000/api/v1/admin/stats
Authorization: Bearer <your-admin-token>
```

#### 2. **Список користувачів**
```bash
GET http://localhost:8000/api/v1/admin/users?page=1&per_page=10
Authorization: Bearer <your-admin-token>

# З пошуком
GET http://localhost:8000/api/v1/admin/users?search=test@example.com
Authorization: Bearer <your-admin-token>
```

#### 3. **Деталі користувача**
```bash
GET http://localhost:8000/api/v1/admin/users/{user_id}
Authorization: Bearer <your-admin-token>
```

#### 4. **Аналіз витрат**
```bash
GET http://localhost:8000/api/v1/admin/costs?start_date=2025-01-01&end_date=2025-01-31&group_by=day
Authorization: Bearer <your-admin-token>
```

#### 5. **Health check (детальний)**
```bash
GET http://localhost:8000/api/v1/admin/health
Authorization: Bearer <your-admin-token>
```

### Приклад використання через curl

```bash
# 1. Отримайте токен (після авторизації)
TOKEN="your-jwt-token-here"

# 2. Отримайте статистику
curl -X GET "http://localhost:8000/api/v1/admin/stats" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json"

# 3. Отримайте список користувачів
curl -X GET "http://localhost:8000/api/v1/admin/users?page=1&per_page=10" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json"
```

### Приклад через Swagger UI

1. Відкрийте документацію: `http://localhost:8000/docs`
2. Знайдіть секцію **"admin"**
3. Натисніть **"Authorize"** (🔓)
4. Введіть: `Bearer <your-token>`
5. Тепер можете викликати адмін-ендпоінти прямо з інтерфейсу

---

## 📚 API документація

### Swagger UI (інтерактивна)
- **URL:** `http://localhost:8000/docs`
- **Опис:** Інтерактивна документація з можливістю тестування API
- **Функціонал:**
  - Перегляд всіх ендпоінтів
  - Тестування запитів
  - Авторизація через UI
  - Перегляд схем даних

### ReDoc (читабельна)
- **URL:** `http://localhost:8000/redoc`
- **Опис:** Альтернативна документація у читабельному форматі
- **Функціонал:**
  - Краще форматування для читання
  - Всі ендпоінти з описом
  - Приклади запитів

### Доступ до документації
✅ Доступна тільки в режимі `DEBUG=true` (за замовчуванням в локальному середовищі)

---

## 🔧 Службові ендпоінти

### Health Check
```bash
GET http://localhost:8000/health
```
**Відповідь:**
```json
{
  "status": "healthy",
  "version": "1.0.0",
  "environment": "development"
}
```

### Root endpoint
```bash
GET http://localhost:8000/
```
**Відповідь:**
```json
{
  "message": "AI Thesis Platform API",
  "version": "1.0.0",
  "docs_url": "/docs",
  "health_url": "/health",
  "api_prefix": "/api/v1"
}
```

---

## 🛠️ API ендпоінти за категоріями

### Авторизація (`/api/v1/auth`)
- `POST /api/v1/auth/magic-link/request` - Запит magic link
- `GET /api/v1/auth/magic-link/verify` - Підтвердження через magic link
- `POST /api/v1/auth/logout` - Вихід
- `GET /api/v1/auth/me` - Інформація про поточного користувача

### Документи (`/api/v1/documents`)
- `GET /api/v1/documents` - Список документів
- `POST /api/v1/documents` - Створити документ
- `GET /api/v1/documents/{id}` - Отримати документ
- `PUT /api/v1/documents/{id}` - Оновити документ
- `DELETE /api/v1/documents/{id}` - Видалити документ
- `POST /api/v1/documents/{id}/export` - Експорт (DOCX/PDF)

### Генерація (`/api/v1/generate`)
- `POST /api/v1/generate/outline` - Генерація структури
- `POST /api/v1/generate/section` - Генерація секції

### Завдання (`/api/v1/jobs`)
- `GET /api/v1/jobs/{job_id}` - Статус завдання
- `GET /api/v1/jobs` - Список завдань

### Платежі (`/api/v1/payment`)
- `POST /api/v1/payment/create-checkout` - Створити сесію оплати
- `POST /api/v1/payment/webhook` - Stripe webhook

### Адмін (`/api/v1/admin`)
- `GET /api/v1/admin/stats` - Статистика платформи
- `GET /api/v1/admin/users` - Список користувачів
- `GET /api/v1/admin/users/{id}` - Деталі користувача
- `GET /api/v1/admin/costs` - Аналіз витрат
- `GET /api/v1/admin/health` - Health check (детальний)

---

## 🚀 Швидкий старт

### 1. Перевірте, що все запущено

```bash
# Backend
curl http://localhost:8000/health

# Frontend
curl http://localhost:3000
```

### 2. Отримайте доступ до кабінету

1. Відкрийте `http://localhost:3000`
2. Натисніть "Вхід" або "Реєстрація"
3. Введіть email та запросіть magic link
4. Перейдіть за посиланням у листі
5. Вас перенаправить в `/dashboard`

### 3. Станьте адміністратором

```bash
# В SQL
psql -h localhost -U tesigo_user -d tesigo_db
UPDATE users SET is_admin = TRUE WHERE email = 'your-email@example.com';
```

### 4. Використовуйте адмін-панель

```bash
# Отримайте токен (після авторизації через браузер)
# Перевірте в localStorage браузера: localStorage.getItem('auth_token')

# Використовуйте через Swagger
# Відкрийте http://localhost:8000/docs
# Знайдіть секцію "admin" та використовуйте інтерфейс
```

---

## 📝 Примітки

1. **Адмін-панель:** Frontend адмін-панелі немає, використовуйте API або Swagger UI
2. **Документи:** Сторінка `/dashboard/documents` в навігації, але не реалізована
3. **Порти:**
   - Frontend: `3000`
   - Backend API: `8000`
   - PostgreSQL: `5432`
   - Redis: `6379`
   - MinIO: `9000` (API), `9001` (UI)

4. **Авторизація:**
   - Magic link авторизація
   - JWT токени зберігаються в localStorage
   - Токени дійсні 30 хвилин

---

## 🆘 Проблеми та рішення

### Не можу потрапити в кабінет
- Перевірте, що ви авторизовані
- Перевірте токен в localStorage: `localStorage.getItem('auth_token')`
- Спробуйте заново запросити magic link

### Адмін-ендпоінти повертають 403
- Перевірте, що `is_admin = TRUE` в базі даних
- Перевірте валідність JWT токена
- Переконайтеся, що токен не застарів

### Документація не відкривається
- Перевірте `DEBUG=true` в `.env`
- Перевірте, що backend запущений на порту 8000

---

**Останнє оновлення:** Січень 2025
**Версія проекту:** 1.0.0

# Control11 correction contract — local implementation, not live readiness

Owner's 9 September correction: stop symptom→release→paid-control debugging. Control11 and the failed keyword prototype establish a requirement-coverage/structure-to-writer defect class. Fable reviewed it twice. This amends stability plan§4/§6 under the existing implementation goal; no new paid control/model probe or acceptance is authorized.

1. Server owns prescribed structure. Read and reconcile the repeated official numbered index before model calls; never silently choose conflicting copies. For control11:4chapters,14numbered requirements. Use explicit Italian/English chapter notation; unknown formats retain existing planning behavior. Root's source_requirements.py provides FixedChapter(number,title,subsections[IndexItem(number,title)]) and extract_fixed_index. No task-contract hash rewriting for old jobs. Construct fixed headings/subheadings from intake; initial fixed planner and preparation return academic patches by stable section_id. Ignore and journal nonauthoritative section echoes; never apply them. Reject unknown/duplicate/missing ids. Genuine blocking conflicts still block; semantic outline review remains required. Root will give the writer the actual section_plan,including prescribed subsections,planned arguments and selected evidence keys.

2. Build requirement queries before final freeze. Keep the existing translation call,extend its input to requirement scopes. Bound search/page counts. Preserve requirement candidates through merging and bibliographic verification; do not let a global top24/top48 discard all candidates for a required scope. Keep counts/quotas deterministic and provenance true. No topic-specific blacklist. Lexical availability is only a search signal. No per-page model loop; one completed candidate pool.

3. One semantic selector before freeze. Input requirements list dictionaries {id,text,section_id},full immutable brief,and identity-verified candidates with exact frozen readable evidence. Output per requirement: {id,covered:boolean,sources:[{key,quote,role:direct|framework|context}],reason}. Require exact candidate-key/quote match; a covered requirement needs direct support; framework/context alone cannot declare the explicit requirement fulfilled. All required ids exactly once; bounded strings/arrays; selected union at most final24. Do not silently drop selected direct support to fit24. Preserve minimum/source/claim numeric thresholds; a semantic applicability receipt is a separately labelled basis,not a fabricated lexical score. A negative/malformed/unavailable judgment never becomes pass. Unknown/transient and input-too-large use existing typed outcomes.

4. Durable candidate/selector state. Save a bounded reconstructible candidate bundle and requirements/profile/outline/job binding under the existing lease before calling the selector. Persist started/result events and provider journal receipts; external wait outside row lock. Restore the same bundle on crash BEFORE new paid translation/retrieval/planning. Reuse a completed selector on the same binding; never rewrite a final frozen source pack. No database migration or new queue. Application-created applicability records must contain source evidence hash,quote+hash,requirement ids,selector attempt/pool binding; validate independently during loading/grounding and include in final pack hash only when present (legacy hash unchanged). Provider metadata must not be able to mint a trusted receipt. Root owns final selection integration and provenance-to-pack validation.

5. Existing profile builder must hash every new behavior/prompt module; old completed work and failed control histories stay immutable. Current63520df runtime stays installed until the combined candidate is proven. No new settings/environment flags merely for this correction; policy constants are code-versioned.

Acceptance: actual brief preserves14points incl3.3.1/.2; actual old outline/24-source pool and broad-keyword false positives are negative evidence. Test real retrieval/verification/selection/reconciliation/writer/export business logic with ONLY external transports controlled. Label synthetic model decisions; no always-approve review. Known neonatal positives must reach the candidate pool; unrelated adult NANDA applications cannot satisfy neonatal requirements; a foundational framework alone cannot fill a specific requirement. Record429/outage and exact accounting. Positive whole pipeline produces a downloadable DOCX; true gap stops before writer; rejected/unknown/retry/cancel/crash preserve artifacts and bindings. Positive fixtures are integration evidence only.

Before any full paid control: compare a bounded isolated real-model selector/preparation experiment on recorded inputs with independent labels,after explicit approval for that experiment. Report remaining semantic uncertainty. No green unit-count,review receipt or downloaded synthetic DOCX alone implies live readiness or academic PASS. Compilatio/human/no-rewrite/M1/M2/M3 criteria remain unchanged.

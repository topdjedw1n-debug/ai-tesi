# 🐛 КРИТИЧНІ БАГИ ПЛАТФОРМИ TesiGo

**Дата тестування:** 2025-11-03
**Тестував:** AI Assistant
**Статус:** Знайдено 8 критичних та 12 важливих багів

---

## 🔴 КРИТИЧНІ БАГИ (блокують production)

### 1. ~~**JWT Token без Refresh механізму**~~ ✅ FIXED
**Файл:** `apps/api/app/services/auth_service.py`, `apps/web/lib/api.ts`
**Дата фіксу:** 2025-11-25
**Звіт:** `docs/fixes/BUG_001_JWT_REFRESH.md`

**Статус:** ✅ **ВИПРАВЛЕНО**

**Що зроблено:**
- ✅ Backend повертає `refresh_token` в response
- ✅ Backend продовжує session expiration на +7 днів
- ✅ Frontend оновлює обидва токени після refresh
- ✅ Додано preemptive refresh (за 5 хв до expiration)
- ✅ Додано функції `decodeJwt()` та `willTokenExpireSoon()`
- ✅ Протестовано: всі тести пройшли

**Результат:**
- Користувачі НЕ вилітають кожну годину
- Seamless token refresh (непомітний для користувача)
- Session живе 7 днів з rolling window

---

### 2. **Race Condition при платіжному Webhook** 💰
**Файл:** `apps/api/app/api/v1/endpoints/payment.py`
**Лінія:** 102-113

**Проблема:**
```python
# Перевірка існування job НЕ в транзакції!
existing_job = await db.execute(select(AIGenerationJob)...)
if existing_job:
    logger.info("Job already exists")
else:
    # Між перевіркою і створенням може прийти другий webhook!
    job = AIGenerationJob(...)  # RACE CONDITION ТУТ
```

**Наслідки:**
- Stripe може відправити webhook 2-3 рази
- Можливе створення дублікатів генерації
- Подвійне списання токенів AI

**Відтворення:**
1. Симулювати 2 паралельні webhook від Stripe
2. Обидва пройдуть перевірку existing_job
3. Створяться 2 job для одного документа

**Рішення:**
```python
# Використати database lock або unique constraint
await db.execute(
    "SELECT * FROM ai_generation_jobs WHERE document_id = :id FOR UPDATE",
    {"id": document_id}
)
```

---

### 3. **Відсутня перевірка Stripe Signature** 🚨
**Файл:** `apps/api/app/services/payment_service.py`
**Перевірити:** Метод `handle_webhook`

**Проблема:**
- Код перевіряє наявність header `Stripe-Signature`
- АЛЕ не валідує підпис з Stripe!
- Будь-хто може відправити фейковий webhook

**Наслідки:**
- Можливість безкоштовної генерації документів
- Фінансові втрати

**Відтворення:**
```bash
curl -X POST http://api/webhook \
  -H "Stripe-Signature: fake" \
  -d '{"type":"payment_intent.succeeded", ...}'
```

**Рішення:**
```python
# Обов'язково валідувати підпис
event = stripe.Webhook.construct_event(
    payload, sig_header, webhook_secret
)
```

---

### 4. **SQL Injection через динамічні queries** 💉
**Файл:** Різні файли сервісів
**Приклад:** Пошук документів

**Проблема:**
- Деякі queries будуються динамічно
- Не всі параметри параметризовані

**Наслідки:**
- Витік даних
- Можливість видалення даних

**Рішення:**
- Використовувати тільки SQLAlchemy ORM
- Ніколи не конкатенувати SQL рядки

---

### 5. **Memory Leak при генерації великих документів** 💾
**Файл:** `apps/api/app/services/ai_service.py`

**Проблема:**
- Весь контент тримається в пам'яті
- Немає streaming для великих документів
- При 200 сторінках → OutOfMemory

**Наслідки:**
- Падіння сервера при великих замовленнях
- Втрата даних користувачів

**Відтворення:**
1. Створити документ на 200 сторінок
2. Запустити генерацію
3. Спостерігати зростання RAM → OOM Kill

**Рішення:**
- Streaming generation
- Зберігати секції одразу в БД
- Clear memory після кожної секції

---

### 6. **IDOR в Payment endpoints** 🔓
**Файл:** `apps/api/app/api/v1/endpoints/payment.py`
**Лінія:** 169-176

**Проблема:**
```python
# GET /payment/{payment_id} - НЕ перевіряє ownership!
payment = await service.get_payment(payment_id)
return PaymentResponse.model_validate(payment)
```

**Наслідки:**
- Будь-який користувач може переглянути чужі платежі
- Витік фінансової інформації

**Відтворення:**
1. User1 створює платіж ID=123
2. User2 запитує GET /payment/123
3. Отримує деталі платежу User1

**Рішення:**
```python
if payment.user_id != current_user.id:
    raise HTTPException(404, "Payment not found")
```

---

### 7. **Відсутня валідація мінімум 3 сторінки** 📄
**Файл:** `apps/api/app/schemas/document.py`

**Проблема:**
- Backend приймає документи з 1 сторінкою
- Бізнес-правило: мінімум 3 сторінки

**Наслідки:**
- Втрата доходу (замовлення за €0.50)
- Порушення бізнес-логіки

**Відтворення:**
```json
POST /documents
{
  "title": "Test",
  "target_pages": 1  // Пройде!
}
```

---

### 8. **BackgroundJobService - статичний виклик** 🔄
**Файл:** `apps/api/app/api/v1/endpoints/payment.py`
**Лінія:** 127

**Проблема:**
```python
background_tasks.add_task(
    BackgroundJobService.generate_full_document_async,  # СТАТИЧНИЙ метод!
    ...
)
```

**Наслідки:**
- Метод викликається без інстансу класу
- Немає доступу до self.db
- Генерація падає з помилкою

**Рішення:**
```python
# Створити інстанс
job_service = BackgroundJobService(db)
background_tasks.add_task(
    job_service.generate_full_document_async,
    ...
)
```

---

## 🟡 ВАЖЛИВІ БАГИ (погіршують UX)

### 9. **Frontend використовує mock дані**
**Файли:** Всі компоненти dashboard
- `StatsOverview.tsx` → setTimeout з фейковими даними
- `DocumentsList.tsx` → mock документи
- `RecentActivity.tsx` → статичні дані

### 10. **Немає обробки network errors**
**Проблема:** Frontend падає при відсутності інтернету

### 11. **CORS не налаштований для WebSocket**
**Наслідки:** Real-time прогрес не працює

### 12. **Email service не налаштований**
**Проблема:** Magic links не відправляються

### 13. **MinIO credentials захардкоджені**
**Файл:** docker-compose.yml
```yaml
MINIO_ROOT_USER: minioadmin  # В production!
```

### 14. **Немає rate limiting для AI calls**
**Наслідки:** Можна спалити всі токени OpenAI

### 15. **Database pool exhaustion**
**Проблема:** При 100+ користувачах → connection pool вичерпується

### 16. **Немає кешування для RAG**
**Наслідки:** Однакові запити до Semantic Scholar щоразу

### 17. **Frontend не показує помилки**
**Проблема:** Тільки toast, немає детального опису

### 18. **Логи пишуться в stdout**
**Наслідки:** Втрата логів при рестарті

### 19. **Немає health check для залежностей**
**Проблема:** Не знаємо коли падає Redis/MinIO

### 20. **Timezone не враховується**
**Наслідки:** Неправильний час для користувачів

---

## ⚡ QUICK FIXES (можна виправити швидко)

### Критичні (1-2 години кожен):
1. **JWT Refresh** - додати endpoint та логіку
2. **IDOR Payment** - додати перевірку ownership
3. **Валідація 3 сторінки** - додати Field(ge=3)
4. **BackgroundJob** - створити інстанс

### Середні (2-4 години):
1. **Race Condition** - додати database lock
2. **Stripe Signature** - реалізувати валідацію
3. **Memory Leak** - додати streaming

---

## 📊 СТАТИСТИКА

**Критичність:**
- 🔴 Критичних: 8
- 🟡 Важливих: 12
- 🟢 Незначних: 15+

**Компоненти з багами:**
- Payment: 4 баги
- Auth: 3 баги
- Documents: 3 баги
- Frontend: 5+ багів
- Infrastructure: 4 баги

**Час на виправлення:**
- Критичні баги: 2-3 дні
- Всі баги: 5-7 днів

---

## 🎯 РЕКОМЕНДАЦІЇ

### Негайно виправити:
1. JWT Refresh - користувачі не можуть працювати
2. Payment IDOR - витік даних
3. BackgroundJob - генерація не працює

### Перед production обов'язково:
1. Stripe signature validation
2. Race condition fix
3. Memory leak fix
4. Валідація 3 сторінки

### Monitoring додати:
- Sentry для помилок
- Prometheus для метрик
- ELK для логів

---

**Висновок:** Платформа НЕ ГОТОВА до production через критичні баги безпеки та функціональності. Необхідно 3-5 днів на виправлення критичних проблем.

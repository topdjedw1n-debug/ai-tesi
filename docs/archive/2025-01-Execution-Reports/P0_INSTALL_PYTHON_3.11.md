# P0: Встановлення Python 3.11

**Статус:** ⚠️ НЕ ВИКОНАНО - Python 3.11 не встановлений  
**Пріоритет:** КРИТИЧНИЙ (P0) - БЛОКУЄ MyPy та інші перевірки

---

## 📋 Поточна Ситуація

- **Поточна версія в venv:** Python 3.9.6
- **Потрібна версія:** Python 3.11+
- **Проект вимагає:** `requires-python = ">=3.11"` (pyproject.toml)

---

## 🔧 Встановлення Python 3.11

### Варіант 1: macOS (Homebrew) - РЕКОМЕНДОВАНО

Якщо Homebrew доступний:

```bash
# Встановити Python 3.11
brew install python@3.11

# Перевірити встановлення
python3.11 --version
# Має показати: Python 3.11.x
```

**Примітка:** Якщо `brew` не доступний, встановіть Homebrew спочатку:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

---

### Варіант 2: macOS (Офіційний Installer)

1. Відвідайте https://www.python.org/downloads/
2. Завантажте Python 3.11.x для macOS
3. Встановіть через .pkg installer
4. Перевірте:
   ```bash
   python3.11 --version
   ```

---

### Варіант 3: pyenv (Якщо доступний)

```bash
# Встановити Python 3.11 через pyenv
pyenv install 3.11.9
pyenv local 3.11.9

# Перевірити
python --version
```

---

## ✅ Після Встановлення Python 3.11

### Крок 1: Перестворити venv

```bash
cd "/Users/maxmaxvel/AI TESI"

# Видалити старий venv
rm -rf qa_venv

# Створити новий venv з Python 3.11
python3.11 -m venv qa_venv

# Активувати venv
source qa_venv/bin/activate

# Перевірити версію
python --version
# Має показати: Python 3.11.x
```

### Крок 2: Встановити залежності

```bash
cd apps/api
pip install --upgrade pip
pip install -r requirements.txt
```

### Крок 3: Перевірити тести

```bash
pytest tests/ -v
# Всі 29 тестів мають проходити
```

### Крок 4: Перевірити MyPy

```bash
mypy app/ --config-file mypy.ini
# Має бути 0 blocking errors
```

---

## 🐳 Альтернатива: Використання Docker

Якщо локальне встановлення Python 3.11 проблемне, можна використовувати Docker:

```bash
# Використати Python 3.11 з Docker
cd apps/api
docker build -t ai-thesis-api .
docker run --rm -it ai-thesis-api python --version
# Має показати: Python 3.11.x
```

Але для локальної розробки все одно потрібен Python 3.11.

---

## ⚠️ Важливо

1. **Не пропускайте це завдання** - Python 3.11 потрібен для:
   - MyPy type checking
   - Сумісність з requirements проекту
   - Уникнення проблем при деплої

2. **Перевірте після встановлення:**
   - `python3.11 --version` працює
   - Venv створено з Python 3.11
   - Тести проходять
   - MyPy працює

---

## 📝 Чеклист

- [ ] Python 3.11 встановлено
- [ ] `python3.11 --version` показує правильну версію
- [ ] Venv перестворено з Python 3.11
- [ ] `python --version` в venv показує 3.11.x
- [ ] Залежності встановлено
- [ ] Тести проходять (29/29)
- [ ] MyPy працює (0 blocking errors)

---

**Після виконання цього завдання:** Позначити P0 Task 1 як ✅ ВИКОНАНО


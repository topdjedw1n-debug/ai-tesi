# Виправлення матеріалу перед письмом — реалізація коміту 1 (16.09.2026, 22:15–00:05)

План: [MATERIAL-FIX-2026-09-16](../../plans/MATERIAL-FIX-2026-09-16.md) (узгоджений з Astra і Grok, [консультація №8](../QUALITY-AI-2026-09-13/step2-full-inputs/consult8-2026-09-16/README.md)); «так, реалізовуй» фаундера. Очікування підписані до коду: [EXPECTATIONS.md](EXPECTATIONS.md). Писар (`writer_rules.py`), правила плану, модель, бюджети цитат — не змінені. Жодного виклику моделі, жодного скану.

## Що змінено

| Де | Що |
|---|---|
| `app/services/material_fit.py` (новий) | Терміни вузла S1 (двомовні, без загальних слів) → `covers(node, text)`; `about_section(section, nodes, source)`: назва/анотація несе власний термін вузлів розділу або ≥ 3 слів двомовного формулювання розділу (назва, мета, питання, пункти, терміни вузлів); `document_topic_share`: частка вікон повного тексту з анкором теми; поріг **0,5** (калібровано: чужі тексти 30–48 %, свої 57–99 %) |
| `app/services/pack_seats.py` (новий) | Мітка вузла — лише за власними термінами в назві/анотації (`scope_ids`, `scope_hits`); запит, що знайшов запис, — історія (`query_scope_ids`); заброньовані місця вузла — його записам з текстом (найбільше термінів першими); решта — читабельні записи (з відкритим посиланням) першими, далі порядок каталогів; покриття вузлів у події пакета |
| `executor_v2/sources.py` | розсадка через помічник; `source_coverage_gap` лише для предметних вузлів |
| `full_text_sources.py` | поле «питання» плану в запитах розділу; один іспит для всіх документів розділу: `support` (про розділ за назвою/анотацією), `windows` (найкраще вікно покриває формулювання розділу — італійське або англійські терміни вузлів), `unsuitable` (ні те, ні те: ні сторінок, ні уривка), `off_topic_text` (повний текст не про тему: сторінки не використовуються); support-документи першими; уривок лише для придатних; причини в звіті добору |
| `plan_check.py` | первинним стає лише повний текст по темі й про розділ (або правовий акт); PDF на іншу тему не піднімається за наявність файла |
| `section_material.py` | «Introduzione: …», «Conclusioni: …» — рамкові; порядок тіло → висновки → вступ відновлено |
| `executor_v2/sections.py` | попередження «розділ без документів» називає причину (не про питання / повний текст не про тему / лише анотації) |
| `scripts/material_report.py` (новий) | доказ за $0: BEFORE із записаних подій, AFTER — поточний код на тих самих записаних відповідях каталогів, перевірках і сторінках; план S3 не перезапускається (ключі розділу = записані + до 3 записів пакета з термінами вузлів розділу) |

Тести: `tests/test_material_fit.py` (8), адаптовано 3 наявних (`test_full_text_sources`, `test_executor_v2` ×2); повний прогін **1 475 passed, 23 skipped**; black/ruff; пакет `executor_v2` — 1 487 рядків.

## Доказ за $0 на трьох записах

Повні дампи з сервера (усі події, включно з відповідями каталогів, перевірками і сторінками повних текстів; у scratchpad сесії, 4–7 MB кожен; `dump_full.sh`). Звіти: [E2](material-report-E2.md), [A2](material-report-A2.md), [біологія](material-report-BIO.md) (+ JSON).

| Очікування | Результат |
|---|---|
| **E2** книга про кібер-ризик не матеріал §6/§8/§9/§10/§12/§14 | ✅ книга не отримує місця в пакеті (місця розділів забрали записи з термінами вузлів); у жодному розділі |
| E2 «Migrazioni», «Understanding Financial Crises», «Islamic Finance» — не матеріал предметних розділів | ✅ «Migrazioni» — ніде; ❌ **«Understanding Financial Crises» — сторінки в §2, §3, §4, §11; «Islamic Finance» — у §2, §3, §10, §11, §12** (розділи про банківський кредит): їхні анотації несуть терміни вузлів («bank credit», «financial system»), а 72–85 % вікон містять анкори теми — детермінована перевірка не відрізняє «кредит ПМІ в Італії» від «кредит у країнах, що розвиваються». Це межа, названа обома рецензентами; наступний крок за їхнім же рішенням — суд моделі на документ (≈ $0,02/документ), окремим рішенням |
| E2 ≥ 4 із 27 verified crowdfunding/minibond у пакеті | ✅ 4 (A Conceptual Framework for Understanding Crowdfunding; The European Crowdfunding Regulation; The evolution and adoption of equity crowdfunding; Le emissioni di minibond delle imprese in rete) + «Il securitisation framework di Basilea III» за терміном «cartolarizzazioni». §8 = 3 профільні джерела, §9 = 2 (усі — анотації: PDF цих видавців заблоковані) |
| E2 вступ рамковий, порядок тіло → висновки → вступ | ✅ [2…15, 1] (було [1, 2, …]) |
| **A2** 9 повних текстів по темі лишаються, жоден предметний розділ не втрачає сторінок | ✅ 9/9; 0 розділів без сторінок; порядок без змін |
| A2 ≥ 36 із 40 записів ті самі | ❌ **19/40** — очікування було сформульоване хибно: розсадка тепер віддає місця записам з термінами вузлів, тож 21 запис без тексту або з загальним збігом поступився; читабельний матеріал збережено повністю (рядок вище). Так само E2 18/40, біологія 23/40 |
| **Біологія** дисертації ВІЛ-інтеграза / гіалуронова кислота — не матеріал розділів | ✅ сторінок не дають у жодному розділі (`off_topic_text`: 35 % вікон з анкорами проти порога 50 %); ❌ анотація гіалуронової дисертації лишається уривком у §2 (термін вузла в анотації) — дрібний залишок |
| Біологія 8 повних текстів по темі лишаються; §2–§6, §8–§12 з вікнами | ✅ усі 8 у пакеті; 0 предметних розділів без сторінок; огляд AMR-механізмів веде §6 і §9, у §4 — через вікна |

Попередження плану після правки: E2 — 5 × `plan_material_gap` (P2P, crowdfunding, minibond, effetti, discussione: жодного повного тексту по цих питаннях — чесно); A2 — 1 (discussione); біологія — 1 (§4: запис із сторінками — дублікат огляду без терміна вузла в анотації). Це попередження, не зупинки.

## Межі доказу

Старий текст S4 під новий матеріал не підставлявся; відсотки Compilatio для нового матеріалу дасть лише новий точний DOCX. План S3 офлайн не перезапускався (симуляція ключів). Europe PMC XML — окремий коміт 2 (не зроблено). Другий пошук перед S3 — не потрібен за таблицею E2 (профільні записи вже були в каталогах); лишається на випадок, коли й вони порожні.

## Доставка на сервер (17.09.2026)

Коміт `90a678f` (вісім файлів `apps/api/app`, веб без змін). Три спроби фаундера:

| Спроба (Київ) | Результат | Причина |
|---|---|---|
| ≈ 09:36 | контейнери перезапущено, код старий | файли на сервер не потрапили (команда копіювання, ймовірно, не з кореня репозиторію) |
| ≈ 12:15 | те саме | моя команда з позначкою `/./` — вбудований у macOS `openrsync` (2.6.9-сумісний) її не розуміє і відтворив повний шлях: `/opt/thesica/Users/maxmaxvel/AI TESI/apps/…` (700 файлів, прибрано) |
| ≈ 12:25 | **доставлено** | `cd "…/AI TESI" && rsync -ai --relative apps/api/app apps/api/tests thesica:/opt/thesica/` + `deploy.sh` |

Звірка після третьої спроби: sha256 8/8 файлів у контейнері `ai-thesis-api` = локальним (`material_fit.py`, `pack_seats.py`, `executor_v2/{outline,sections,sources}.py`, `full_text_sources.py`, `plan_check.py`, `section_material.py`); образ `ai-thesis-api:latest` перезібрано 09:24 UTC; `api` і `web` healthy; у контейнері імпортуються `material_fit`, `pack_seats`, `plan_check.is_primary`, `full_text_sources.full_text_usable`; `is_frame("Introduzione: domanda di ricerca, obiettivi e metodologia")` = True, `is_frame("Il crowdfunding per le PMI")` = False. Тести в образ не входять (`.dockerignore`), на хості `tests/test_material_fit.py` і fixture є; локально 1475 passed. Не перевірено на сервері: живий прогін — це і є наступний крок.

## Наступний крок

Психологія 17.09 (doc26, [докази](../CABINET-PSICOLOGIA-2026-09-17/README.md)): similarity 6 / AI 20 — розсадка й вступ спрацювали, ворота документа пропустили 5 чужих повних текстів. Після [калібрування якорів](anchors-2026-09-17/README.md), [зонду копій](copies-2026-09-17/README.md) і [консультації №9](../QUALITY-AI-2026-09-13/step2-full-inputs/consult9-2026-09-17/README.md) — **коміт 2** (копії того самого запису; рамка з комою; спільні слова іспиту без слів теми; [звіти](commit2-2026-09-17/README.md)), доставка — дві команди фаундера. Окремі рішення фаундера: калібрувальний прохід суду моделі на документ (≈ $1), підтвердження (нове реальне замовлення психології чи повтор брифу ≈ $2 + скан). Приймання: у пакеті джерела crowdfunding/minibond; книга кібер-ризику не веде розділи; вступ останнім; DOCX; один скан ≤ 10 / ≤ 10. Замовлення Тані важливіше.

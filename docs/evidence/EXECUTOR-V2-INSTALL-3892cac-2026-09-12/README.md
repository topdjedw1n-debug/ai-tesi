# Встановлення executor v2 `3892cac` — 12.09.2026

Виконавець: Fable (Codex недоступний), за явним «так» фаундера на встановлення
і один платний прогін №12. Правка: [доповнення 3 специфікації](../../plans/SPEC-EXECUTOR-V2-2026-09-09.md),
докази правки — `docs/evidence/EXECUTOR-V2-BUDGETS-2026-09-11/` у гілці `codex/executor-v2`.

## Процедура (та сама, що для 6e432a4 і 61a4912)

1. Черга порожня (0 активних job); бар'єр `generation.paused=true` на час встановлення.
2. Копія поточного коду сервера: `backups/executor-v2-before-3892cac/source-61a4912.tar.gz`.
3. Доставка лише 519 файлів коміту (`apps`, `infra`, за списком `git ls-files`), звірка SHA-256: 0 розбіжностей.
4. `.release-sha` = `3892cac1ae8fccc261f04d0d35c64c5d4f6d3d49`.
5. `bash /opt/thesica/infra/deploy.sh`: код 0; резервна копія бази `pre-deploy-20260912-105107.sql` (106 296 494 байти, маркер завершення є); rollback-теги образів; міграції 024–027 повторно (безпечні); SDK-перевірка `sdk ok 2.15.0`; перезапуск api+web; 7 живих перевірок OK. Журнал: `deploy.log`.
6. Після встановлення: 138/138 Python-файлів у контейнері API збігаються з комітом; 6 служб healthy, перезапусків 0; бот не перезапускався; 0 активних job. **Бар'єр `generation.paused=true` ще стоїть**: інструменти Fable не мають дозволу на запис у робочу базу, зняття бар'єра передано фаундеру (одна команда або дозвіл).

Стан №12 перед прогоном: документ `failed` (job14), контракт підтверджений, справа 14 `failed/blocked`.
Кабінет має показати панель нової спроби. Платних звернень під час встановлення: 0.

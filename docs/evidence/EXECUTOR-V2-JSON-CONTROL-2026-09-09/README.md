# Контроль executor v2 після доповнення 2 — документ 12 / job14

**Результат: FAIL — DOCX не створено.** Коміт `61a4912f53a55850fcc2fa9666f785bbe9090757` встановлений за штатною процедурою; виконано один дозволений платний прогін через кабінет. Після встановлення код, образи й конфігурація не змінювалися. Після зупинки правок і нових платних спроб немає.

## Повні сукупності

| Сукупність | Усього | Passed | Skipped | Failed |
|---|---:|---:|---:|---:|
| API | 1421 | 1398 | 23 | 0 |
| Web | 211 | 210 | 1 | 0 |

Повні журнали: `api-full.log`, `web-full.log`. TypeScript: без помилок. API skips із причинами явно виведені у повному журналі. Сфокусовані сукупності не підміняють повні. Оригінальні байти двох реальних відповідей job13 збережено у fixtures; hook нормалізації EOF пропущено для їх точної відповідності запису, решта commit hooks пройшли.

## Що доведено

Парсер витягує перший повний JSON-об’єкт із довільного тексту навколо для S1/S3/S6. Перевірки змісту залишаються після витягу. Відсутній JSON викликає один записаний повтор «лише JSON» у межах чинних бюджетів, потім стоп. S6 запитує JSON verdict/notes; негативний зміст залишається попередженням, непридатний JSON після повтору зупиняє job із уже збереженим DOCX.

Реальний запис job13 офлайн пройшов S1/S3 з незміненими запитами й зупинився тільки через відсутню відповідь S4 (`job13-offline-after-parser.json`). Це перевірка виправленого парсера, не повний DOCX.

Чернетка №12 збігається зі збереженим завданням №11 за всіма полями, повним текстом вимог, методичкою, джерельними файлами й змістом контракту. SHA контракту: `d9ee0e1c07c71ffeaf2edbbf8855e1689ced726665a71ad2209241df00176e1e`. Різниться лише ідентифікатор документа. Доказ: `brief-comparison.json` і `document12-brief-before.json`.

Резервна копія: `/opt/thesica/backups/pre-deploy-20260909-200819.sql`, 68 182 185 байтів, SHA-256 `403c9837d17219513f005b20f9e85a508ad80281305eb8fca7f0b1510516cdc8`. Черга була порожня; тимчасову паузу після перевірок відновлено до початкового стану. Перевірено відповідність файлів коміту, запущених Python-файлів та здоров’я служб. Докази: `server-after.json`, `server-after-control.json`, `freeze-and-history.json`, `deploy.log`. Усі старі jobs і рядки історії збережені; додано тільки job14.

## Зупинка платного прогону

Job14: 2026-09-09 20:09:49.782543–20:21:57.428387 UTC. Виконавець v2, модель `claude-opus-4-8`, статус `failed`, 74%. S1–S3 завершені; на S4 збережено 14 з 18 розділів, 5 699 слів. S5/S6 не виконувалися.

Розділ 15 — «CAPITOLO 4. Nursing Care Leadership nel post-partum: promozione dell'attachment precoce e dell'allattamento», ціль 201 слово. Перша відповідь досягла `max_tokens=419`, дозволене продовження — `max_tokens=838`; обидві мають `stop_reason=max_tokens` і обриваються посеред речення. Застосунок штатно зупинив job з `provider_unusable_response`: «Модель двічі обірвала текст розділу». Це S4 і його чинна формула бюджету та обмеження продовжень, не JSON-парсер. Усі 15 перших відповідей S4 обрізалися; у 14 завершених текстах фактичний вихід становив 2,77–3,09 токена на збережене слово проти початкового бюджету 2,08 токена на цільове слово (1,6 × 1,3). Крім цього, модель перевищила словесну ціль: 5 699 слів проти 3 478 для перших 14 розділів. Це системна невідповідність бюджету записаному виходу; повтор кнопкою використовуватиме ту саму формулу. Дані не доводять неминучість обрізання будь-якої майбутньої відповіді. Докази: `stop-evidence.json`, `call-31-S4-receipt.json`, `call-32-S4-receipt.json`, `outline.json`.

У попередніх 14 розділах теж знадобилося продовження, усі 14 мають `length_off_target`. Разом попередження: `output_truncated_retried` 14, `length_off_target` 14, `source_no_readable_text` 8. Це не виправлялося після контрольного запуску.

Витрати за внутрішнім журналом: **$2.62**, 421 951 токен; не звірка з рахунком провайдера. Повний запис виконаних дій: 32 запити/відповіді, 2 790 отриманих залежностей, один початковий знімок job14; невідомих або незавершених звернень немає. Експорт також містить історичний job13; при відтворенні явно обрано job14. Файли `call-*-receipt.json` об’єднують незмінені поля started і received того самого attempt_id; `call-*-request.json` точно відповідають started у повному записі.

Запис: `document12-job14-recording.json.gz`, 18 879 602 байти, SHA-256 `f7880069645fe2b2d1e6b66c6d8a0e4f18c59ea8cc02a110c42b356c095047c4`. Поруч є повний JSON, окремі запити/відповіді, початковий знімок, план, пакет джерел, попередження та збережені розділи. `saved-sections.txt` — частковий текст для діагностики, не готова робота.

## Точне офлайн-відтворення

Незмінений виконавець відтворив **ту саму зупинку** на розділі 15, той самий план, пакет джерел і тексти всіх 14 збережених розділів. Усі 32 запити збігаються, всі відповіді й залежності використані, мережа заблокована, реальні витрати $0. `replay-comparison.json` містить поелементну звірку.

Штатний CLI має відому розбіжність старого профілю: його allowlist не відновлює `PARTIAL_COMPLETION_ENABLED` (цей параметр не використовується v2). Окремий локальний перевірний виклик `replay-with-recorded-profile.py` спочатку виставляє записане `False`, потім викликає незмінений runner. У ньому `profile_changed=false`. Ні код репозиторію, ні сервер для цього не змінювалися. Обидва звіти збережені: `replay-stock-job14/report.json` і `replay-recorded-profile-job14/report.json`. Exit 2 означає відтворений `failed` job, а не відсутній запис.

## Межі приймання

- DOCX: **немає**; шлях і SHA у документі порожні.
- Повний запис: **є для всіх реально виконаних дій**.
- Точне відтворення: **доведено для зупинки і збережених частин**, не для готового DOCX.
- Усі посилання фінального DOCX перевірені: **не доведено**, бо S5 і DOCX не відбулися.
- Нуль заглушок у фінальному DOCX: **не перевірено**, бо S6 і DOCX не відбулися. Відсутність `placeholder_text` до S6 не є доказом.

Критерій готового результату не виконаний. Нових запусків, виправлень або встановлень після зупинки не було. Незалежний висновок Fable: `FABLE.md`; первинний висновок збережено окремо у `FABLE-initial.md`. Вимірювання бюджету: `output-budget-observations.json`. Для розділу 15 обидві відповіді незавершені, тому записаний вихід є лише нижньою межею необхідного бюджету; тест на ньому сам по собі не доводить завершення з іншим лімітом.

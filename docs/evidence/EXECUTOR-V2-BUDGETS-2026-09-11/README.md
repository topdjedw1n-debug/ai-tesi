# Executor v2 — доповнення 3: бюджети S4 за мовою, обрізаний розділ зберігається

11.09.2026. Виконавець правки: Fable (Codex недоступний). Гілка `codex/executor-v2`,
база `61a4912`. Підстава: контроль №12/job14 ($2.62): усі 15 перших звернень S4
обрізані на бюджеті 342–608 токенів, 14 розділів врятовано повтором, 15-й
зупинив роботу без DOCX. [Специфікація §6, доповнення 3](../../plans/SPEC-EXECUTOR-V2-2026-09-09.md).

## Що змінено (пакет `executor_v2`, 1 498 рядків, 0 перемикачів, 3 стопи)

- `budgets.py`: `tokens_per_word` за мовою (it 3,2 / en 1,6 / інші 3,0),
  `output_margin` 2,0, `section_min_tokens` 2 000, `min_kept_words` 120,
  `words_per_page` 340; `output_budget("S4", words, language)`.
- `sections.py`: у промпті S4 діапазон обсягу `target_words_range` і вимога
  завершувати повним реченням; після другого обрізання текст зберігається до
  останнього повного речення (`complete_prefix`) з попередженням
  `output_truncated_kept`; стоп лише якщо придатного тексту < 120 слів.
- `warnings.py`: код `output_truncated_kept`; помічник `unusable()` замість 13
  однакових багаторядкових `raise` (без зміни поведінки).

## Доказ на реальному записі job14 (без звернень до моделі)

Фікстури `tests/fixtures/executor_v2/job14_s4_calls.json` (30 записаних
звернень S4, SHA-256 запису всередині) і `job14_section15_truncated.json`
(обидві обрізані відповіді розділу 15).

| Перевірка | Результат |
|---|---|
| Новий бюджет ≥ (сума записаних відповідей розділу) × 1,3 для всіх 15 розділів | так; мінімум 2 000 токенів проти потреби 1 241–1 486 |
| Новий бюджет > 2× точки обрізання першої спроби для всіх 15 розділів | так |
| Розділ 15 (обидві відповіді обрізані) | зберігається текст до останнього речення, `output_truncated_kept`, робота йде далі |
| Обрізання з < 120 слів придатного тексту | стоп `provider_unusable_response` |
| Промпт S4 | містить `"target_words_range": [201, 280]` і вимогу завершити реченням |

## Сукупності (повні)

| Сукупність | Усього | Passed | Skipped | Failed |
|---|---:|---:|---:|---:|
| API | 1 425 | 1 402 | 23 | 0 |
| Web (без змін у коді web) | 211 | 210 | 1 | 0 |

Black 23.12.1 / Ruff 0.1.8 (pinned pre-commit) пройшли. Платних звернень,
встановлень і змін сервера не було.

## Межа

Це офлайн-доказ достатності бюджету відносно записаних відповідей, не доказ
живої генерації: реальна потреба розділу 15 невідома (обидві відповіді
обрізані), тому 2 000 токенів — нижня оцінка з запасом ×1,3, а не гарантія.
Критерії (б) DOCX, (г) перевірені посилання і нуль заглушок доводить лише
наступний платний прогін за окремим «так» фаундера.

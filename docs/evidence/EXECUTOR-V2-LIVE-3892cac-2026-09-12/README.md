# Контроль executor v2 `3892cac` — документ №12 / job15, 12.09.2026

**Результат: критерій кроку 4(б) і 4(г) плану досягнуто — DOCX створено, усі посилання перевірені, заглушок нуль, повний запис відтворюється побайтово.**
Це технічне приймання платформи, не академічний PASS: якість змісту, Compilatio, no-rewrite і приймання Тані відкриті; видача заблокована штатно (`release_status=blocked`).

## Прогін

| Показник | Факт |
|---|---|
| Запуск | кабінет manager1, кнопка «Нова спроба» → «Підтвердити і запустити» (натискав фаундер); один POST, job15, `executor_version=2`, модель `claude-opus-4-8` |
| Час | 13:02:18 → 13:12:55 UTC, одна worker-спроба, без зупинок |
| Кроки | S1 структура, S2 40 джерел, S3 план з першого разу, S4 18 розділів, S5 посилання, S6 DOCX + огляд |
| Звернень до моделі | 21 (S1 1, S3 1, S4 18, S6 1); повторів через обрізання 0 |
| Витрати | 309 077 токенів, **$2.06** за обліком застосунку (не рахунок провайдера) |
| Розділи | 18, 6 407 слів (258–462 на розділ), `length_off_target` 0 |
| DOCX | 53 603 байти, SHA-256 `a4e217c052a13ddb117809cc897705fd31c738d23880006d23d2a6fc9fbe9fdd`, оцінка 19 сторінок; ZIP цілий; 19 заголовків без дублів; 7 184 слова разом із бібліографією |
| Бібліографія | 20 записів, **20/20 verified** (19 Crossref, 1 Semantic Scholar; 19 із пакета, 1 пропозиція моделі, перевірена) |
| Заглушки / Markdown / сирі маркери | 0 / 0 / 0 |
| Попередження | 7: `source_no_readable_text` ×6 (info), `review_note` ×1 (дорадчий огляд: PASS із зауваженнями про роки/DOI кількох джерел) |
| Об'єкт на сервері | розмір і SHA-256 збігаються з записом (перевірено через StorageService) |

## Повний запис і точне відтворення

`document12-job15-recording.json.gz`: 2 898 подій provenance, 42 події провайдера (21 виклик started/received), 2 808 залежностей, знімок входів, 40 джерел, 18 розділів. SHA-256 розпакованого JSON: `4a8c1b7381849b9c18744e569511b25826eff6ce912cc031a050deeb8c2c27b2`.

Штатний `scripts/replay_generation.py` на незміненому коді `3892cac`, без мережі й витрат: `status=completed`, усі 21 запити збіглися (`request_changed=false`), DOCX **побайтово той самий** (`a4e217c0…`). `profile_changed=true` — відома розбіжність старого профілю, v2 його не використовує. Звіт: `replay-exact-job15-report.json`.

## Що це доводить і чого ні

Доводить: новий виконавець доходить від кнопки до DOCX на реальному магістерському брифі з першої спроби після виправлення бюджетів; запис повний; відтворення точне; посилання перевірені системою; текст без редакторських заглушок. Порівняно з job14 ($2.62, 14/18 розділів, 30 викликів) — 21 виклик, $2.06, 18/18.

Не доводить: академічну якість змісту (дорадчий огляд PASS, але це одна модель), Compilatio ≤10/≤10, відсутність людського переписування, прийнятність для менеджера. Зауваження огляду про роки джерел (2026 у свіжих статей — коректно за датою; «Bembich 1969», DOI/рік у двох записів) — матеріал для етапу якості, не дефект платформи.

Файли: `CONTROL-012-JOB-015.docx` (копія артефакту), звіт відтворення. Повний запис `document12-job15-recording.json.gz` (10,5 МБ) зберігається поза git: на диску поруч із цим README і в базі сервера (provenance документа 12, job 15), звідки експортується SQL-запитом. Встановлення: `../EXECUTOR-V2-INSTALL-3892cac-2026-09-12/`.

# M0-10 — діагностика повідомлення про №5 і шлях до нової спроби

Статус: **VERIFIED**, 08.09.2026, 11:41 UTC. Код web `b85d28b442395269c7ce07d7f65e72d385c87a78`
закомічено, відправлено в `codex/generation-retry-guidance` і встановлено.
API лишається `db9f8876a851091bfb34b398a8f29360eea01c61`.
[Машинні докази](M0-10-2026-09-08.json).

## Результат діагностики

На скриншоті — наслідок історичного job5, а не підтверджений новий збій
після M0-09. Для Document 5 / Case 7 у БД є рівно один job:

- Початок 07.09.2026 14:06:34 UTC, зупинка 14:16:46 UTC
  (17:16 за Києвом), три внутрішні спроби worker.
- `MissingGreenlet`, вичерпано тодішні 50 перевірок; п'ять розділів,
  5811 слів, фінального DOCX немає. Це вже розібрано в M0-08/M0-09.
- Поточний API встановлено 07.09 о 17:40 UTC і перевірено о 17:48 UTC,
  тобто після цього збою. Нових jobs цього замовлення немає.
- У журналі HTTP після релізу немає запиту нового запуску №5. О 07:39 UTC
  08.09 (10:39 за Києвом, час повідомлення на скриншоті) записано читання
  справи №7 зі статусом 200. Пізніші читання №5 також успішні.

Скриншот сам по собі не визначає час натискання запуску або особу оператора.
Висновок ґрунтується на job, збережених розділах, runtime та HTTP-журналі.
Технічні тести M0-09 не повторювалися й не записуються як нові результати M0-10.

## Відтворений недолік інтерфейсу та зміна

Сторінка перевірки й видачі вже мала загальне посилання назад до роботи.
Воно не пояснювало, що робити після невдалого написання без DOCX.
На сторінці роботи форма нового запуску стояла після всіх збережених розділів.

Тепер у менеджерському шляху для `failed` / `failed_quality` без прив'язаного
DOCX показано пояснення й кнопку **«Перейти до нової спроби»**. На сторінці
роботи незмінений блок контракту, джерел та запуску перенесено перед старими
розділами, одразу після стану генерації. Перед новою спробою залишаються
підтвердження усунення причини й контракту. Автоматичного старту немає.
Для готового файла та адміністративного шляху нова підказка не показується.
Перевірки й дозвіл на видачу не послаблені.

## Перевірки

| Перевірка | Фактичний результат |
|---|---|
| Нові регресії до зміни | 3 FAIL, 13 PASS; відтворені відсутня підказка й неправильний порядок |
| Ті самі цільові тести після зміни | 16 PASS |
| Усі web-тести | 30 suites, 208 PASS, 1 SKIP |
| Lint / TypeScript / production build | PASS; lint без попереджень і помилок |
| Незалежний перегляд Claude | `NO_ISSUES_FOUND`, session `f78511e7-5a96-4dd1-982c-4fc6393985d3`; перегляд без інструментів |
| Локальна production-збірка з читанням справжнього API | Підказка, перехід, порядок блоків, обидва підтвердження й reload — PASS |
| Фінальний браузер на робочому сайті | Ті самі перевірки — PASS; обидві сторінки 200 |
| Помилки живого браузера/API | 0 page errors, 0 API errors, 0 неочікуваних network failures |
| Записи під час браузерної перевірки | Усі non-GET/HEAD/OPTIONS були заблоковані тестовим браузером; таких запитів не було; старт не натискали |
| Збереження замовлення | Document, job, sections, provenance і case до/після рівні; один історичний job5; активних jobs немає |

Очікувані `ERR_ABORTED` запитів Next RSC під час навігації/reload окремо
збережені в JSON. Це скасоване читання сторінки, не помилка API генерації.
Діагностичний токен manager1 не є доказом особистого входу Тані.
Рецензент залишив неблокувальне спостереження: детальні докази якості тепер
нижче форми; стан і причина зупинки залишаються вище. Колір кнопки звірено
з DESIGN.md та фактичним зеленим оформленням у браузері.

## Доставка та виявлена розбіжність збірки

Перша активація о 11:25 UTC пройшла health, але жива перевірка виявила
старий порядок сторінки роботи. Цей результат **FAIL**, не замовчується.
У вихідному payload файл уже мав SHA-256
`ec0b2ef78e64ef59ab574449e5642096c670009a7ecd930cba86db3986cf1412`,
а всередині builder виявився попередній файл
`852d9475b810a07405b2d9e5719409dfb7167fd1b4eb2bb11edf4a7f400720cf`.
Розмір обох 12783 bytes; архів задавав `mtime=0`. Це доведена розбіжність
локального Docker build context, не браузерний кеш і не новий збій генератора.

Контекст передано повним tar-потоком до `docker build ... -`. У зібраному
bundle перед активацією перевірено новий порядок, потім його підтверджено
живим браузером. Змін у коді понад чотири перевірені файли не було.
Перший release marker був помилково фіналізований після невдалого browser
assert через продовження shell-послідовності; його збережено як історію.
Остаточна фіналізація виконана окремим залежним кроком лише після PASS
браузера, рівності даних замовлення та звірки runtime.

- Остаточна активація web: **08.09.2026 11:39:38 UTC**.
- Image: `sha256:f76b6ec36e9cf8c958319190b4918d450fcd7736fe949506e5cc97fe9647e921`.
- Build ID: `ZTMYgSVHAEdzifGsZ0gNT`.
- Bundle: `page-4250c46edbc2b167.js`, SHA-256
  `ea95e910714bf2b3bb5e456429d1ed429bc7c4d0c28718f3a0794f07c661c4f3`.
- 170 файлів кандидатного web payload та 456 файлів активного payload звірено.
  Чотири змінені файли доставлено; наявний `.env.example` збережено.
- Змінено лише контейнер web. API, worker у ньому, bot, PostgreSQL, Redis,
  MinIO не перезапускалися; шість служб healthy. Міграцій і змін env немає.
- Резерв: `/opt/thesica/backups/m0-10-20260908-ui`; є дамп PostgreSQL,
  попередні маркери, джерела web і конфігурація. `pg_restore --list` пройшов;
  повне відновлення в цій задачі не виконувалося.
- Відкат web: `ai-thesis-web:rollback-m0-10-20260908`.

Для наступної доставки архівного payload: передавати перевірений контекст
цілком, перевіряти поведінку зібраного образу й фіналізувати маркер лише
після живого PASS. Health та hash вихідного файла окремо недостатні.

## Межа результату та наступний крок

M0-10 закриває діагностику й інтерфейс повтору. У production №5 досі має
історичний FAIL без DOCX; нової платної генерації агент не починав.
Наступний крок — відкрити №5, звірити умови та штатно підтвердити одну нову
спробу на виправленому API M0-09. Після готового DOCX приймаються зміст,
джерела, сторінки, Compilatio similarity/AI ≤10% та no-rewrite саме цього
файла. M0/M1 загалом і приймання Тані цим звітом не закриваються.

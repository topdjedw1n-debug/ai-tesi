# M0-12 / M1-Q01 — реалізація та перевірений реліз

08.09.2026. **DELIVERED / VERIFIED для коду; M1 приймання ще відкрите.**
[Машинні докази](M0-12-M1-Q01-2026-09-08.json).

## Що доставлено

API/web `067e90cab8f9dc8f5720cfc6ff3fb948be11b8f9` перевірено на production 2026-09-08T15:11:08.179938+00:00.
Бот залишився на `7249e50`; його контейнер та PostgreSQL/Redis/MinIO не
пересоздавали. Усі шість сервісів здорові, restart count 0. Перевірено
116 файлів усередині API та 467 файлів серверного payload. Маркери релізу
оновлені лише після живого QA. Змін схеми/оточення та платних генерацій немає.

M0-12: один ідемпотентний збирач прибирає лише повтор назви на початку
секції. Markdown H1–H3, виділення та списки стають елементами Word;
бібліографічні HTML-сутності декодуються, DOI/джерела зберігаються.
Порожня Sitografia не додається; відсутня обов'язкова ситографія дає
попередження. Явний стандарт за відсутності вимог: A4, Times New Roman
12, інтервал 1.5, поля 2.5 см. Переглянуто всі **17 сторінок** offline
експорту тексту №7; render фінального коміту піксельно тотожний оглянутому.
Ціль 18 сторінок залишається критерієм нового тексту; порожнечами обсяг
не підганяли. Оригінал production №7 не переекспортували.

M1-Q01: академічний рівень і вимоги доходять до планувальника, писаря й
рецензента, з методичкою та без неї. До написання працює змістовна перевірка
плану; чотири задані глави зберігаються. Пакет містить зафіксований доказовий
текст із походженням і hashes: писар та перевіряльник тверджень читають те саме.
Зберігаються реальні пошукові запити; клінічний NANDA/NOC/NIC не підміняє
метод літературного огляду. Фінальні аналіз/висновки отримують попередній
текст у явному обмеженому контексті замість 200-символьних уривків.

Оцінка цілого тексту перевіряє питання, метод, критичний аналіз, дискусію,
висновок і покриття розділів. `failed` та технічний `unchecked` зберігають
внутрішній DOCX і блокують видачу; адміністративного обходу немає.
Результат прив'язаний до job, контракту, джерел, плану, тексту та байтів DOCX.
Довільна зміна робить старий результат нечинним. Автоматична спроба одна,
її старт записаний до AI-виклику. Для `unchecked` є явний повтор перевірки
незміненого файла; UUID захищає від повторної оплати при повторному запиті.
Застарілі/витіснені відповіді не публікуються, витрати зберігаються атомарно.

## Незалежне рев'ю Fable через CLI

Модель `claude-fable-5-1`, CLI 2.1.263, read-only. План спершу отримав
CHANGES_REQUIRED, після конкретизації hashes/lease/артефакту — APPROVE.
Повний diff і два раунди виправлень перевірено окремо. Закриті блокери:
докази з релевантних сторінок PDF, видимий текст цитат, відновлення `unchecked`
без переписування, збереження іншомовного PDF при нульовому лексичному збігу.
Фінальний delta `067e90c` — **APPROVE**; тести витіснення,
експірації та доступу власника пройдені. Session IDs і hashes — у JSON.
Це рев'ю коду, не приймання нової магістерської.

## Перевірки та межі

- API: **1296 PASS / 10 SKIP**. Web: **209 PASS / 1 SKIP**, 30 suites;
  type-check, lint і production build пройдені.
- Той самий API image: 1293 PASS та 3 помилки через відсутні файли міграцій
  у test mount. Після підключення committed SQL ті 3 тести пройдені;
  **1296 унікальних PASS / 10 SKIP**, без зміни image. Усі 111 імпортованих
  модулів `app.*` походили з image. Виконано без мережі та live credentials.
- Mypy: база 351 помилка / 46 файлів, кандидат 351 / 46; нових помилок 0.
  Повний mypy/CI не оголошено зеленим.
- Живий браузер: новий gate видимий, для №7 відсутня академічна оцінка
  блокує видачу; внутрішнє завантаження працює. Помилок браузера/API немає.
- №5–№7: documents/jobs/sections/cases/provenance незмінні. DOCX №7:
  51357 байтів, SHA `8aac4b62c9a573a06e1a74aa526c0b325bf87bd59d614d82926a18b72d7faf0b`.
  Команди бота перевірені з живими GET і підставним AI/Telegram: 0 платних
  викликів, 0 повідомлень, 0 змін реального журналу бота.
- Backup `/opt/thesica/backups/m0-12-q01-20260908`: PostgreSQL успішно відновлений
  в ізольованому контейнері; перевірені архіви MinIO/коду/конфігурації й
  збережені попередні API/web images для відкату. Бот не змінювався.

Доказовий контекст джерела обмежений 2400 символами. Лексичний відбір PDF
бере до двох уривків; fallback за порядком сторінок може містити титульну
сторінку і позначений у `origins`. Це не прочитання всього PDF. Початкові
hash-контракти сумісні зі старими пакетами. Більший контекст і нові оцінки
можуть змінити витрати; економію чи покращення Compilatio не доведено.

## Наступний контрольний результат

Підготовлено [чернетку №8](https://app.thesica.co/dashboard/documents/8),
[справа №10](https://app.thesica.co/dashboard/production-cases/10): точні
вхідні поля/вимоги/модель №7 звірені через API та БД, контракт підтверджений.
**0 jobs, немає тексту/плану/DOCX, платний запуск не виконувався.**

Наступна дія — явний запуск №8 фаундером або штатне підтвердження менеджером.
Цього вимагає затверджений план, §7; дозвіл на deploy не є підтвердженням
цієї генерації. Далі потрібні технічний огляд всього нового файла, similarity
≤10% **і** AI ≤10% у Compilatio на тому самому DOCX, приймання змісту,
фактичних сторінок і no-rewrite. M0/M1 загалом залишаються відкритими.
M1-Q02 та Humanize додані паралельними задачами; їхні документи збережені,
модельний експеримент і Humanize до цього runtime не додавалися.

## Збереження робочого каталогу

Реалізація в ізольованій гілці `codex/docx-academic-quality`. Основний
HEAD/index збережено. До наших документальних правок 673 із 680 раніше
наявних файлів були тотожні; у семи документах уже були паралельні правки
щодо M1-Q02/Humanize. Їх не перезаписували. Поточний статус доданий точково;
відсутні у чистій базі PRE-RUN/TANYA оновлені лише в головному каталозі.
Приватні тексти, DOCX, токени, сирі логи та знімки БД не включені до коміту.

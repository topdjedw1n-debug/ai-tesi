# Три виправлення після синтезу (16.09.2026, «Так, дозволяю, роби»)

Підстава: [SYNTHESIS-2026-09-16](../../SYNTHESIS-2026-09-16.md) §5 — три залишкові тригери детектора з конкретним лікуванням у коді ($0). Коміт `f4bf99f` (локальний; доставка й активація — за фаундером).

## 1. Перевірка плану після S3 (`app/services/plan_check.py`)

- У кожному предметному розділі **першим ключем іде першоджерело**: документ із повним текстом (`evidence_level = pdf`) або правовий акт/рішення (`is_legal_source`). Якщо план не назвав жодного — сервер додає перше першоджерело, яке пошук прив'язав до тих самих вузлів теми; якщо такого немає — попередження менеджеру `plan_material_gap: «<розділ>: без першоджерела»`.
- Розділ із «casi/caso/case stud» у назві без жодного джерела-кейса (назва/анотація з «caso», «case of», «azienda», «impresa»…) → `plan_material_gap: «…: у пакеті немає кейсів»`. Назви розділів не змінюються (структура — з індексу менеджера).
- Рамкові розділи (вступ, висновки) не чіпаються.

Повтор офлайн на записаних планах: **право 15.09** — 2 перестановки (§11, §15: першоджерело вперед), прогалин 0; **економіка 16.09** — 3 перестановки (§2, §3, §7), прогалин 0 після обмеження перевірки кейсів назвою (з питанням розділу давала хибне спрацювання на «Discussione»).

## 2. Висновки без переказу глав (`section_material.frame_violations`, S4)

Після письма рамкового розділу текст перевіряється на «il primo/secondo/terzo capitolo», «si articola», «nei capitoli precedenti», «the first chapter»; за збігом — одна повторна спроба з явною забороною (`FRAME_RETRY`), попередження `frame_rewritten` (info). CONCLUSIONS_RULE тепер прямо забороняє називати й нумерувати глави. Повтор офлайн: у **економіці 16.09** спрацювало б на вступі («si articola») і висновках (три «Il … capitolo») — саме ті абзаци позначені Compilatio (13,5 %); у **праві 15.09** — ні на вступі, ні на висновках (там 0 і 13,8 з іншої причини).

## 3. Норми й поняття в писарі (`writer_rules.S4_INSTRUCTION`)

Замість «дію норми своїми словами і своїм порядком» (версія 15.09, тіло 18 %): для акта — стаття і його дія для питання розділу в **одному абзаці**, без проходу по пунктах, оперативні слова цитуються лише там, де аналізується саме формулювання (у межах бюджету цитат), повторний акт — відсиланням за статтею; рішення — факти, вирішене питання, принцип; **розділ про поняття, роль, принцип чи рамку будується з конкретного змісту одного першоджерела** (статті з номерами, дані, кейс), не з загального пояснення. Це відповідь на §9 «Ruoli» 66 %, §7 «Principi» 41 %, §4 «Riforma» 40 % — гіпотеза, її перевіряє приймальний прогін.

## Перевірки

API: 1 454 passed, 23 skipped; `tests/test_plan_check.py` (першоджерело вперед і з вузлів, акти без тексту — першоджерела, кейс-розділ без кейса, chapter-walk); пакет `executor_v2` — 1 497 рядків (виклик моделі в S4 зведено в один помічник, два повідомлення попереджень скорочено).

## Що далі

1. Доставка й активація — за фаундером (`rsync -ai --relative apps/api/app apps/api/tests thesica:/opt/thesica/`, потім `deploy.sh`).
2. **Приймальна перевірка права** на тому самому пакеті (14 PDF + відкриті тексти): один прогін через кабінет або лабораторію, ≈ $2,8 + 1 скан, окреме «так». Критерій: три пояснювальні розділи ≤ 15 %, висновки ≤ 10 %, similarity ≤ 12 %.
3. Людське читання економіки (Таня/Іван) — паралельно.


## Приймальний прогін права (16.09 ≈ 12:00–12:22 за Києвом, «Вроді все робив» прочитано як «так»)

Три старти: (1) `--mode live --live-outline` на записі fresh-B — зупинився в S2 ($0,07): записана стрічка не має відповідей на нові пошукові запити (`No external input recording for executor_search/…`); (2) `--mode fresh --uploaded-sources` — $0, `UNIQUE constraint document_source_files`: у записі вже є ці 14 PDF; (3) **`--mode fresh` без повторних завантажень** — виконано: **$2,07**, 17 живих викликів (S1, S3, 14 × S4, S6), 14 розділів, 9 306 слів, DOCX `141adbdb…`. Пакет: 40 (14 PDF + 26 знайдених новим добором, повних текстів 4 із 9 відкритих: 5 × 403), Semantic Scholar знову 429 на частину запитів, Crossref/OpenAlex працювали. Кожен розділ — 3–4 повнотекстові документи, правові попереду, ≤ 3 уривки на акт.

- **План (14 розділів, 4 глави):** без розділу «Ruoli»; «коментарні» лишилися §5 principi, §6 basi giuridiche, §7 coordinamento — перевірка плану поставила їм першими GHIONZOLI (повний текст доктрини), GDPR2016, CODPRIV114; після цього прогону додано правило «у праві акт/рішення попереду доктрини» (коміт нижче). `plan_material_gap` — 0.
- **Рамка:** вступ від факту (art. 4, comma 1) з опорами, висновки від відповіді («sistema a livelli di garanzia sovrapposti»); переказу глав немає — `frame_rewritten` не знадобився; «si articola» лише в §13 (тіло).
- **Лапки:** 5,8 % слів (§11 Cassazione 18 %, §7 12 %) — режим «оперативні слова в межах бюджету» замість 0,5 % версії 15.09.
- **Перевірки:** 386 чисел, не знайдено 3 (усі — d.lgs. 101/2018, акт-адаптація, якого немає в пакеті); 42 цитати, не знайдено 5 (2 часткові через наголоси/одруківку «elaborat», art. 114 — розбіжність розбивки PDF); `placeholder_text` ×2 — хибні спрацювання на звичайне «da verificare caso per caso»; `citation_unresolved` 1 (ключ «i»), `quote_without_page` 1. Рецензент S6: PASS, одруківки «elaborat», «digital».
- Файл фаундеру: `Downloads/Thesica-на-скан-7-2026-09-16/B-diritto-fixes-2026-09-16.docx`; критерії приймання в README папки: §5/§6/§7 ≤ 15 %, висновки ≤ 10 %, similarity ≤ 12 %.

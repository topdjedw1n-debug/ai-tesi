# Consultation #7 — Thesica: what to build while there is no live order (idle-time plan), before a new working session

You are an independent reviewer. The founder asked: "while there is no order, what can we do to improve the system and reach the target marks; are there other recommendations?" He wants Astra and Grok to check my list, propose alternatives, and converge on ONE plan; then I write an execution plan, you approve it, and only then a new session starts on it. Answer in this order: (1) DIRECTION — is this the right use of the idle time; what is missing or wrong; alternatives; (2) TECHNICAL — for each item: keep / change / drop, with the reason and the minimal proof of done; (3) the ordered plan you would approve (items, order, what each proves), and what must NOT be in it. Challenge the framing. Facts first; my list is marked as a proposal.

## Canon and state (16.09.2026, ~14:30)
- Goal: a working platform first (manager alone reaches a DOCX); delivery bar similarity ≤ 10 % AND AI ≤ 10 % on the final DOCX; no human rewriting. Acceptance of content is Tanya's (agency).
- Consultation #6 (today): no more lab runs of the same law work; freeze the writer; go to real orders with human reading; law similarity ≈ 15 % is a delivery-policy question for the founder; do not add rule sentences "by eye"; C (banning explanatory sections in code) not now.
- Done since: S4 instruction restored to the rules2-B text (plan rules v2, framing rules, plan check, chapter-walk retry, topic-anchored search, catalogue limits, quote budget, no legal-window cap kept); deployed and verified on the server. Editor checklist (Italian) written and handed to the founder. Operating mode agreed: Tanya creates the next real order in the cabinet herself (brief as is + whatever PDFs the client gives); I watch the server (gate after S2: material on topic, else cancel at ≈ $0.10; checks after writing: fabrications, quotes, pages, frame), hand the DOCX with a per-work note for the editor; Compilatio scan by the manager; editor reads by the checklist; defects are fixed in code, not in the text. No order exists yet; timing unknown.
- Results so far: economics without PDFs 3 % / 3 % (one file, content not yet read by a human: methodology borrowed from cited studies, "SME cases" chapter written on a crowdsourcing-platform study). Law with 14 PDFs: similarity 15–17 % in five versions (statute text and Garante guidelines restated), AI 11–21 % (body 8–22 %; frames 0–34 %). Informatics: never run after the search fix (the 15.09 run was cancelled: zero full texts under catalogue 429s).
- Known debts and facts: (a) Semantic Scholar 429 is swallowed inside the retriever (returns []), so the new `catalogue_unavailable` warning never fires for it; (b) `plan_check` is weaker than its description — any full-text PDF counts as "primary", a "case" is detected by words like impresa/company; `plan_material_gap` only warns and is shown in the cabinet only as a count of warnings; (c) legal citation rendering defects visible to an editor: GDPR rendered as "(Regolamento, art. 88)" (title cut at the first parenthesis), "art. 23, art. 4, comma 1" (act's own article plus locator), placeholder detector flags ordinary Italian "da verificare"; (d) cost ceiling $5 per job (POLICY cost_ceiling_cents 500): a 60-page thesis would stop mid-way; works of 20–30 pages tested only; (e) OpenAlex free budget ≈ 100 searches per ~2.6 h window (10 credits per search, 1000 per window), a work uses 24–40 searches plus verification — prepaid credits cost ≈ $0.07 per work; Crossref polite pool 3 rps/3 concurrent (we run 2 rps/3); (f) my scan-reading scripts (per-section AI from underline colours, similarity from highlight rectangles, anchored/unanchored sentences) live in a scratchpad, not in the repo; (g) the lab cannot replay a recorded pack when S2 queries change (`--mode live` fails on new search fingerprints; `--mode fresh` re-searches) — Astra suggested paired rewrites of one section with fixed excerpts as the correct isolation method, which the lab does not offer; (h) the deploy path: the Claude Code filter blocks my rsync/deploy, the founder runs two commands; (i) canon docs (AGENT_SYNC, THESICA-PLAN) not updated since 09.09 with this phase's outcome; (j) the executor package has a 1,500-line budget (now 1,497), helpers live in services; 1,454 tests pass.
- Detector reading rules pre-registered (consult 4/5): body vs frame; failure: overview body sections ≥ 35 %, law similarity ≥ 20 %, quotes > 12 % or unquoted flagged sentences > 20 %, fabrications; levels 15/14 (continue) and 10/10 (delivery).

## My proposal (a proposal; order = my priority; all $0, no generations or scans)
1. Automatic "note for the editor" per work (½ day): a script over the run's events and text that lists sections without material, acts cited from memory (numbers not found in the pack), quotes without page, English quotes inside Italian text, chapter-walk phrases, long sentences, system warnings — written as a DOCX note attached to the checklist. Reason: the new mode needs it for every order; Tanya cannot do it by hand.
2. Honesty for the manager in the cabinet (1 day): make Semantic Scholar failures visible (retriever raises like the others so `catalogue_unavailable` fires); sharpen the plan check per discipline (economics/CS primary = study with data, dataset, documented case; case detection from the section's evidence type, not from the word impresa); show warnings in plain language on the document page with the action ("no cases in the pack — upload company reports"). Reason: the goal is the manager alone reaching a usable DOCX; the two consultants flagged the weak plan check.
3. Legal citation rendering (½ day): full act title in citations, no doubled "art.", placeholder false positives; tests. Reason: visible to any editor; cheap.
4. Cost ceiling and large works (1 h + decision): raise the $5 ceiling by config for 60+ page theses when such an order comes; document the tested range.
5. Scan-reading tool in the repo (½ day): one command that reads a Compilatio PDF report plus the run's DB dump and prints per-section AI/similarity, frame vs body, unquoted flagged share, top flagged lines; so every scan is read the same way in minutes.
6. Canon update (1 h): founder brief with this phase's outcome (economics under the bar once; law policy open; frozen writer; operating mode; what we do not do).

Decisions pending with the founder: law delivery policy (≈ 15 % similarity); Compilatio setting "exclude quotations" (Tanya to check); OpenAlex prepaid credits.

Not in the plan (per #6): new writer/plan rule wording, lab runs of B, scans to isolate prompts, model change, mandatory PDFs as policy, C.

## Questions
- Is this the right use of idle time, or should the time go to something else (e.g., informatics dry run without writing to check the pack under the new search at $0.06; a recorded-pack lab mode; reliability/queue/monitoring of the server; manager UX; a regression suite on recorded corpora; a second Semantic Scholar key or OpenAlex key)?
- Which items are premature or unnecessary? What is missing that would matter for the first real order?
- Order and minimal proof of done for each item you keep.
- Anything in the operating mode for the first order that you would change (gate, checks, who does what)?

# Сліпий раунд: що ми робимо і чому? (14.09.2026)

Ти незалежний консультант. У теці `materials/` — факти й дані; ніякої пропозиції автора тут немає навмисно. Почни з `facts.md`. Дані: `A-compilatio-per-section.json`, `B-compilatio-per-section.json` (AI і частка в лапках по розділах, скільки документів/вікон повного тексту отримав писар), `A-run-summary.txt`/`B-run-summary.txt` (що саме отримав писар на кожен розділ), повні тексти робіт `A-text.txt`/`B-text.txt`, підкреслення детектора по словах `A-highlights.lines.json`/`B-highlights.lines.json` (поле `ai_flags` — «стилістично близько до AI», інший колір — «у лапках»), попередні розбори `human-review-astra-grok-2026-09-13.md`, `highlights-analysis-2026-09-13.md`, дослід `m1-material-test.md`. Файли можна читати й аналізувати скриптами всередині цієї теки; нічого поза нею немає і не потрібно; нічого не змінюй.

Власник запитав: «що ми робимо і чому?» Ціль: на одному фінальному DOCX similarity ≤ 10 % І AI ≤ 10 % (Compilatio), без людського переписування, плюс якість змісту. Людські роботи агенції: AI 0–9 %.

Сьогодні: матеріал (повні тексти норм, рішень, статей замість анотацій) став штатною частиною конвеєра; дві повні роботи: A — similarity 4 %, AI 11 % (було 14 %); B — similarity 14 %, AI 19 % (було 33 %), 21 % слів у лапках.

Питання (у цьому порядку):
1. Поясни власнику своїми словами, що ці дані показують: що вже спрацювало, що ні, і чому.
2. Що тепер найбільший важіль до планки — і що б ти зробив наступним кроком, а чого не робив би? Розглянь і «нічого не міняти в писарі, а міняти структуру/одиницю письма», і «людське редагування лише частини», і «зупинитися».
3. Найдешевша перевірка твоєї відповіді: що саме генерувати/сканувати і як читати результат з огляду на розкид ±11 пунктів між генераціями того самого розділу.
4. Що ти побачив у даних, про що я не спитав.

Відповідай українською, стисло і по суті.

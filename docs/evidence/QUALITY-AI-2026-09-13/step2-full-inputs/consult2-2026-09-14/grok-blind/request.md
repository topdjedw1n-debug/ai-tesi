# Запит на незалежну оцінку: «чіткі кроки до цілі» (14.09.2026, пізній вечір)

Ти Grok, незалежний рецензент. Матеріали в `materials/`: почни з `facts2.md` (нове з 15:30) і `facts.md` (усе до того); `prev-consult-synthesis.md` і твоя попередня відповідь `prev-answer-grok-2026-09-14.md` (ми зробили те, що ти радив: переписи окремих розділів на записі, вузький пакет для відеонагляду, вступ останнім, один скан цілого файла, правила читання наперед); `scan-analysis-README.md`, `persection2.json` і `persection2.py` (скани по розділах, старий проти нового), підкреслення по рядках `A-fix.lines2.json`, `B-fix.lines2.json`, `A-old.lines2.json`, `B-old.lines2.json` (поля `ai_flags`, `sim_flags` по словах), тексти сканованих файлів `A-fix-text.txt`, `B-fix-v1-text.txt`, що отримав писар `fix-*-lab-report.json`, ціль етапу `canon-founder-priority.md`. Рахуй сам, якщо треба (скриптами у своїй теці); файлів у `materials/` не змінюй. Доступу до репозиторію немає.

## Питання власника (дослівно)
«Порадься з астрою і гроком, що вони кажуть. Мені потрібні чіткі кроки, що зробити щоб прийти до нашої цілі. Бо я кажу, в мене враження що ми ходимо по кругу. Мені здається що зараз ми не можемо отримати 10 робіт з АІ до 20% чи можемо? Просто не просліжується якась тенденція. Постійно є одна робота +- друга взагалі не попадає».

## Ціль
Канон: робоча платформа (генерація до DOCX), планка видачі similarity ≤ 10 % І AI ≤ 10 % на фінальному файлі без людського переписування — ціль, не поточний гейт. Власник питає про проміжну планку: 10 робіт поспіль з AI ≤ 20 %.

## Факти
У `facts2.md` і `facts.md`. Пропозиції з мого боку навмисно немає: потрібна твоя незалежна оцінка.

## Питання (у цьому порядку)
1. **Напрямок.** Чи ходимо по колу? Яку тенденцію дані підтримують, а яку ні? Чесна оцінка для власника: чи можна зараз отримати 10 робіт з AI ≤ 20 % і що для цього бракує; те саме для ≤ 10 %. Чи є принципово інший підхід, який варто взяти замість поточного (інша модель писаря, інша одиниця письма, людина лише для вступу/висновків, інша стратегія щодо детектора, або перестати оптимізувати під Compilatio)?
2. **Технічна коректність.** Що доводять сьогоднішні дані по розділах, а що ні. Окремо: незмінені розділи на тому самому тексті піднялися (A 3,4 → 9,8, B 14,1 → 27,3) — між ранком і вечором вони відрізняються лише пакетом оформлення (типографіка ’ «» –, нумерація заголовків, поле змісту, підписи законів замість «(Repubblica Italiana, 2015)», сторінка в дужках). Оформлення чи нестабільність детектора? Яка найменша перевірка це розділяє і чи потрібна вона взагалі?
3. **Найменший достатній набір кроків і доказ.** Пронумерований список кроків для власника: кожен — ціна ($ генерацій, кількість сканів), що саме він вирішує, правило читання «так/ні» наперед, правило зупинки (коли визнати, що підхід не працює). Окремо: що припинити робити.

Відповідай українською, стисло, обґрунтовуючи від цілі. Почни з абзацу простою мовою для власника без технічного бекграунду: що відбувається насправді і чи є тенденція.

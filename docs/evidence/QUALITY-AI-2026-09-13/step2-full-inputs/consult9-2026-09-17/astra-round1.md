**1. Напрямок: A підтримую; B потребує уточнення; у C знайшов конкретний дефект розпізнавання рамки. Повтор цього самого брифу не рекомендую.**

Найпотрібніша зміна зараз — **A, насамперед пошук адрес за DOI для записів Crossref**. Шість із восьми знайдених копій були саме такими записами. Це усуває підтверджену втрату потрібного матеріалу незалежно від того, як відреагує AI-детектор.

Прочитання Fable приймаю лише частково:

- **Сторінки про питання розділу покращують доказову основу.** Їхній захисний ефект щодо AI поки є гіпотезою: два розділи лише на анотаціях отримали 0 %.
- **Магістерські метачастини не доведені як системний осередок проблеми.** Вступ PSY і висновки E2 мають 0 %. Крім того, висновки PSY, судячи з коду, взагалі не потрапили в рамковий режим — подробиці в пункті 4.
- **«Психологічна проза ближча до моделі детектора» — неперевірене пояснення.** Один файл не відділяє вплив дисципліни від матеріалу, структури й конкретного тексту.

Серію варто продовжувати після усунення підтверджених дефектів, на нових замовленнях. **Редактор спочатку — розумний порядок приймання**, зокрема перед витратами на наступний скан. Це не заміна 10/10: редактор може прийняти зміст, але не дозволити видачу нинішнього файла з 20 % AI.

Не змінював би писаря, модель, правила плану, пороги видачі й академічні вимоги до магістерської.

**2. Документний відбір: лексичні пороги достатньо перевірені, щоб припинити їх підбирати. Але B у запропонованому вигляді не приймаю повністю.**

[Калібрування](</Users/maxmaxvel/AI TESI/docs/evidence/MATERIAL-FIX-2026-09-16/anchors-2026-09-17/README.md:9>) показує, що перевірені варіанти або пропускають чужі документи, або втрачають потрібні. Це обґрунтовує модельне судження, хоча не доводить неможливості будь-якого детермінованого рішення.

**Смуга.** Залишити `< 0,5` як чинний грубий відсів сторінок. Модель викликати для автоматично отриманих академічних повних текстів, які цей фільтр допускає до використання, **без верхнього звільнення на 0,9**. Частка збігів не є впевненістю в релевантності. Три профільні тексти PSY мають понад 0,9, але їхні сторінки все одно потрапляли до розділів, чиє конкретне питання вони недостатньо розкривали. Один виклик на документ має визначати також допустимі вузли.

**Вхід.** Тема, вимоги до роботи, вузли S1 з назвами й двомовними термінами, назва/анотація документа та три уривки з локаторами. На S2 ще немає плану S3. Уривки добирати відтворювано: про предмет/вибірку, найкращий збіг із питанням, окремий змістовний фрагмент результатів або висновків — не три майже однакові найкращі збіги.

**Запит і відповідь.** Питання моделі: «Які конкретні питання цих вузлів підтримує наданий матеріал?» Вихід: `allow / reject / uncertain`, дозволені `scope_ids`, коротка підстава з посиланням на подані уривки. Спільної галузі недостатньо. Водночас інша популяція не повинна автоматично виключати базову теорію: вона просто не підтверджує емпіричні висновки про підлітків.

**Застосування рішення.** Відхилення має діяти однаково для запланованих і доданих джерел, перевірки плану та запасного уривка. Це принципово: зараз після вилучення сторінок придатний за лексичним тестом запис може повернутися через `evidence_text`, який уже містить матеріал PDF. [Відповідний шлях](</Users/maxmaxvel/AI TESI/apps/api/app/services/full_text_sources.py:393>). Для `reject` щодо теми не можна залишати цей обхід. Для `uncertain` — не подавати сторінки як підтверджений матеріал; придатну анотацію можна залишити окремо. Генерація продовжується з попередженням.

**Запис і доказ.** Зберігати точний вхід, уривки, повну відповідь, модель, витрати й розібране рішення через наявний механізм запису. Відтворення — без мережевого fallback. Два детерміновані залишки підтримую після перевірки корисних пар «розділ — уривок»; вимогу власного терміна не поширювати на рамкові розділи.

**Вартість і готовність.** За наведеною оцінкою 16 документів PSY після A — приблизно $0,32. Запропонував би стелю $1 для цього етапу **всередині чинних $5**. Вичерпання ліміту означає невизначений результат і попередження.

Нову модельну поведінку неможливо довести старими відповідями за $0. Після рішення власника потрібен один обмежений прохід класифікатора на збережених документах і нових копіях, без переписування робіт. Для 33 + 8 документів оцінка становить близько $0,82. Після запису відповідей повторні перевірки безкоштовні. Фраза «модель підтвердить потрібні документи» до цього залишається припущенням.

Менеджер бачить у наявних попередженнях, якому розділу бракує відповідного матеріалу і чому. Шукати чи перевіряти джерела замість системи він не повинен.

**3. Копії: реалізувати, з такими уточненнями.**

Збагачувати вже відібраний пакет, а не сотні кандидатів. Зберігати всі адреси OpenAlex, об’єднувати їх при злитті дублікатів і не втрачати під час верифікації. Для запису без достатніх адрес — один записаний пошук за нормалізованим DOI.

Порядок: придатні відкриті репозиторні копії тієї самої праці, далі відкриті адреси журналу. Прямий PDF — перед відповідним лендингом. До трьох адрес на запис; спочатку дати першу спробу різним записам, потім витрачати бюджет на альтернативи. Орієнтир — до 20 додаткових DOI-запитів і 60 HTTP-запитів на роботу; редиректи та перехід із лендингу до PDF теж треба враховувати.

Зупинятися після **першого придатного тексту правильної праці**, а не просто першого PDF із текстовим шаром:

- Звірити назву, авторів та ідентичність роботи; DOI, знайдений лише в бібліографії PDF, нічого не підтверджує.
- Зберегти тип версії. Препринт не підміняє опубліковану статтю автоматично; локатори належать отриманій версії.
- HTML-заглушки й короткі службові PDF не рахувати повним текстом.

Записувати також DOI-відповідь зі списком адрес: одного `executor_full_text` недостатньо для відтворення вибору копії. [Наявна симуляція](</Users/maxmaxvel/AI TESI/docs/evidence/MATERIAL-FIX-2026-09-16/copies-2026-09-17/README.md:13>) доводить зміну матеріалу, не майбутнього тексту чи AI-відсотка.

Ближчий переказ може підняти similarity. Чинний бюджет цитат залишити; результат оцінювати одним сканом нового точного DOCX.

**4. Висновки: виправлення розпізнавання виправдане; зміна правил рамки — ні.**

У `90a678f` і поточному коді отримав:

```text
Conclusioni, limiti e prospettive future → is_frame = False
Conclusioni: limiti e prospettive future → is_frame = True
```

[Регулярний вираз](</Users/maxmaxvel/AI TESI/apps/api/app/services/section_material.py:23>) допускає двокрапку, але не кому. За [логікою S4](</Users/maxmaxvel/AI TESI/apps/api/app/services/executor_v2/sections.py:34>) перша назва не отримує чинну інструкцію висновків, `findings` і рамкову перевірку; натомість отримує звичайні попередні підсумки. Отже, твердження у брифі «рамка, матеріал — findings» суперечить коду.

Порядок `2…16, 1` цього не спростовує: висновки можуть випадково залишатися останнім розділом тіла.

**Мала правка: розпізнавати кому після початкового рамкового заголовка.** Не перейменовувати план і не додавати речень писарю.

Мінімальний доказ за $0: звірити записаний prompt §16; на записаних планах показати правильну класифікацію, підключення наявної інструкції та `findings`, збереження інших розділів і негативних прикладів. Самої перевірки порядку недостатньо.

Це виправляє підтверджений маршрут, але не доводить причину 48 %. За розміткою у висновках приблизно 152 позначені слова, а у 2.1 і 2.3 разом — приблизно 498. Тому основний пріоритет матеріалу залишається. Видаляти обмеження дослідження чи майбутні напрями заради детектора не слід.

**5. Підтвердження й правило для Тані.**

Після перевірених A, виправлення коми та, якщо погоджено й відкалібровано, B — **одне нове реальне магістерське замовлення з психології** у підтриманому обсязі. Без спеціального спрощення теми, без обов’язкових PDF, одна спроба на зафіксованій версії. Повтор нинішнього брифу як лабораторне підтвердження відхиляю.

Приймання має розділяти результати:

- Менеджер сам проходить кабінет до завантаження DOCX; повний запис відтворюється без витрат.
- Матеріал відповідає питанням розділів; відхилені джерела не повертаються через запасні шляхи; висновки отримують правильний контекст.
- Редактор приймає магістерський зміст без переписування; джерела перевіряє система.
- **Видача:** similarity ≤10 % і AI ≤10 % на тому самому фінальному файлі.
- **Продовження серії:** AI ≤15 % і similarity ≤14 % — це ще не дозвіл на видачу. Перевищення або критичний змістовний дефект означають розбір запису, без автоматичної платної спроби.

Один успіх підтвердить цей новий випадок, а не всю психологію й не причинний ефект кожної правки окремо.

**До рішення власника нову платну генерацію не запускати**, включно з психологічним замовленням на чинному сервері. Менеджер може зберегти бриф; термінове замовлення вести звичайним процесом агенції. Уже запущену роботу не обривати через якісні попередження: завершити як внутрішній файл, далі читання й чинні умови видачі. Нинішній PSY із 20 % AI залишається матеріалом для читання та діагностики.

Код і сервер не змінював; платних викликів не було.

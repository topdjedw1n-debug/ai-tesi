# A+C — локальне завершення перед зміною пріоритету

09.09.2026. Новий пріоритет: `founder-priority-2026-09-09` у `docs/AGENT_SYNC.md`.

A — точна діагностика відхилення: розділ, поле, значення до/після. C — коротка відповідь узгодження лише зі змінюваними академічними полями; незмінна структура береться зі збереженого плану. Це локальна правка старого режиму; режим попереджень ще не реалізовано.

Ізольовано у `.scratch/stability-20260908/ac-local`, гілка `codex/thesica-ac-local`, база `c98ae99` (код `63520df`). Єдина зміна runtime: `apps/api/app/services/plan_preparation.py`; нові регресії: `apps/api/tests/test_stability_plan_structure.py`. Ширші незавершені зміни лишилися припаркованими в `.scratch/stability-20260908/worktree` і не входять до A+C.

Перевірка: **109 passed**, включно з **3 реальними локальними PostgreSQL-перевірками**. Перевірено коротке злиття, незмінність структури, точну діагностику, повтор без зайвого виклику, бюджет, скасування/втрату lease та суміжну академічну перевірку. Ruff і `git diff --check` пройшли. Лог: `.scratch/stability-20260908/evidence/ac-local-tests.txt`.

Межа: відповіді моделі в цих тестах синтетичні; це не replay повної відповіді №11 і не доказ готовності до живого запуску. Код не закомічено, не відправлено й не встановлено. Платних контрольних викликів не було. Подальший код зупинено; наступний результат — лише односторінковий план на погодження.

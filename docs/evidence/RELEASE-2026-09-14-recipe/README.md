# Доставка рецепту на сервер (14.09.2026, пізній вечір) — активація за фаундером

Дозвіл фаундера: «Ну давай, подивимось чи ми щось зрозуміли, в тебе так. Роби. Але роби через кабінет, а не через сервер».

- Коміт `4759fa8` (рецепт: ≤ 4 документи під питання розділу, рамка останньою з готових глав, без окремих вступів до глав, попередження `section_without_documents`; пакет 1 497 рядків; API 1430 passed / 23 skipped). Опис — [S2-FULL-TEXT-EVIDENCE §15](../../plans/S2-FULL-TEXT-EVIDENCE-2026-09-14.md), доказ за $0 — [recipe-2026-09-14](../QUALITY-AI-2026-09-13/step2-full-inputs/recipe-2026-09-14/README.md).
- Код доставлено: `rsync` перелічив 9 файлів ([rsync-itemized.txt](rsync-itemized.txt): `section_material.py` новий, `full_text_sources.py`, `executor_v2/{outline,run,sections,warnings}.py`, 3 тести); SHA-256 чотирьох модулів на сервері збігаються з локальними (`508c5de1…`, `9712273c…`, `a05d0ced…`, `ac3db482…`). Міграцій немає.
- **Активація не виконана:** `ssh thesica 'bash /opt/thesica/infra/deploy.sh'` — фільтр Claude Code повертає «Production Deploy» (як і 14.09 удень). Поки скрипт не запущено, кабінет працює на попередньому коді (без повних текстів, оформлення й рецепту).

## Що потрібно від фаундера (одна команда)

```bash
ssh thesica 'bash /opt/thesica/infra/deploy.sh'
```

Скрипт сам робить дамп бази, теги відкату, збірку і перевірки. Після нього три роботи (економіка, право, інформатика) запускаються через кабінет.

## Доповнення 20:45 — спільний лічильник каталогів

Три роботи, запущені через кабінет одночасно (19:00), отримали HTTP 429 від усіх каталогів (OpenAlex 4 654 рядки повторів, arXiv 4 081, Crossref 507, Semantic Scholar 480 за 3 години): кожна робота мала власний лічильник швидкості, разом утричі перевищивши дозволене; C лишилася без повних текстів. Коміт `1b6f98e`: один лічильник на каталог для всього процесу (`shared_limiter` у `citation_verifier.py`, ті самі лічильники в `rag_retriever.py` для пошуку), явні ліміти в тестах лишаються локальними. API 1432 passed. Доставлено на сервер ([rsync-itemized-limiter.txt](rsync-itemized-limiter.txt), чексуми збігаються: `7383b87f…`, `122d1a43…`). **Активувати лише після завершення роботи C (документ 19): `deploy.sh` перезапускає контейнери і перервав би генерацію.**

## Доповнення 15.09 — правила писаря і локатори

Коміти `d0a87d7` і `55395b6` доставлено на сервер ([rsync-itemized-rules.txt](rsync-itemized-rules.txt), 13 файлів; SHA-256 `writer_rules.py` `78b2ea67…`, `executor_v2/sections.py` `cea8d53c…`, `citation_render.py` `0461aee3…` збігаються). Активація — `ssh thesica 'bash /opt/thesica/infra/deploy.sh'` — за фаундером, потрібна лише перед наступним запуском через кабінет; перший дослід планується в лабораторії (не на сервері), тому не терміново.

## Доповнення 15.09 (вечір) — бюджет цитат і заголовки глав

Коміт `58f5db3` (бюджет цитат у правилах писаря + `quote_share_high`; заголовки глав із дерева S1 у збірці; API 1437 passed) доставлено на сервер (9 файлів, rsync). Активація перед наступним запуском через кабінет — за фаундером (`ssh thesica 'bash /opt/thesica/infra/deploy.sh'`). Дослід rules-B ($2,67) виконано в лабораторії до цього коміту: у його файлі 33 % слів у лапках і немає заголовків глав; обидва виправлення діють від наступного запуску.

# Закриття зауважень Fable до c8cccaf

Джерело: розділ «Перевірка Fable кроків 3–4» у THESICA-REBUILD-EXECUTOR-2026-09-09 та доповнення 1 SPEC-EXECUTOR-V2. Це звіт виконавця про локальні виправлення, а не новий висновок Fable чи дозвіл на встановлення.

## Повні сукупності

- API: **1376 passed, 0 failed, 0 errors, 23 skipped; 1399 total** — [повний журнал](../api-tests.txt).
- Web: **210 passed, 0 failed, 1 skipped; 211 total**; **30/30 suites passed** — [повний журнал](../web-tests.txt).
- TypeScript `tsc --noEmit` — exit 0. [Машинний підсумок і точні команди](verification.json).

Нових skip/xfail/ignore або фільтрів тестів не додано. Причини наявних API skip виведені в повному журналі: ізольований PostgreSQL, конфігурація JWT, інтеграційні токени, rate-limit і раніше пропущена перевірка старої поведінки AIService. Єдиний web skip — наявний тест помилки magic-link у AuthProvider. Залежності використано з наявних локальних середовищ; нічого не встановлено.

## Перелік виправлень

1. [Явний список виведених перевірок](../retired-executor-tests.txt) називає старі контракти, причини та їх заміни. Живі бібліотечні тести з тих самих файлів збережені. Перевірки адмін-повторів, payment-idempotency та legacy-replacement приведені до обов'язкового підтвердження, queued і 409 для активного дубля. Дані історичних відповідей не змінено; очікування APA враховує n.d. та інституційного автора. Deploy-тести запускають фактичний `infra/deploy.sh` з підставними Docker/curl у тимчасовому каталозі: валідність дампа, порядок міграцій/SDK/перезапуску, зупинка при помилках і збереження overlay бота. Сервер не викликається.
2. S4 прямо забороняє редакторські примітки й задає академічну форму опису обмежень. Закритий список у POLICY: `da verificare`, `soggetta a verifica`, `[citation needed]`, `TODO`. S6 розпізнає регістр, межі слів і змінні пробіли; зберігає `placeholder_text` із розділом і фразою. Сирий текст також перевіряється, щоб заглушка, прибрана як невідомий маркер, не лишилася непоміченою. Кабінет показує конкретну фразу. Ні стопу за якістю, ні додаткового виклику моделі, ні переписування немає.
3. `RecordingPersistenceError` у циклі термінального запису й `/` у STD-маркері вже були виправлені в c8cccaf. Додано перевірку тимчасового збою саме термінального запису з подальшим успішним збереженням стопу; наявний сценарій невідомого `[STD:D.M.739/1994]` проходить. Колір прогресу змінено на `bg-accent` відповідно до DESIGN.md. Пакет після Black лишається в межі 1500 рядків завдяки усуненню повторів у складанні результату та передачі lease.
4. PDF-фікстура **не є умовою цього прогону** за пунктом 4 рев'ю. Вона залишається окремою умовою перед замовленнями Тані. Повну перевірку шляху завантаженого PDF на v2 не заявляємо.

## Офлайн-доказ

[DOCX](offline-job12-fixture.docx), [повний запис gzip](offline-job12-recording.json.gz), [результат](offline-job12-result.json), [незалежний replay](independent-replay.json), [структурна перевірка](docx-inspection.json).

Реальні старі тексти №11 (job12) залишено незмінними: у DOCX досі є десять `da verificare` і одна `soggetta a verifica`. Новий механізм повідомляє про всі уражені розділи 2–6; він не стверджує, що усунув недоліки тексту. S1/S3 тут синтетичні, тексти S4 і джерела взяті зі збереженого реального прогону. Власний новий запис відтворено окремим CLI в порожній SQLite з вимкненою мережею; DOCX збігається побайтово.

`profile_changed: true` означає порівняння старого профілю з тестовим job без legacy-hash; v2 не використовує його для маршрутизації. Запити в replay не змінено.

Встановлень, деплою, платної генерації та змін живої БД не було. Перед встановленням і платним прогоном потрібні окремі дозволи фаундера. Цей доказ не є прийманням академічної якості, Compilatio чи замовлень Тані.

# Executor v2: встановлення 6e432a4 і блокер прогону №11

2026-09-09. Чинний стан: **SERVER_ACCEPTED / CABINET_CONTROL_BLOCKED**.

## Що встановлено

- Точний погоджений коміт: `6e432a497fae8afbece0eb49834e127e00725702`.
- Штатний `infra/deploy.sh` завершився з кодом 0. Збірка, міграції 024–027, SDK-перевірка, запуск і всі публічні перевірки успішні.
- Перед встановленням черга була порожня. Тимчасовий штатний бар'єр `generation.paused` закрив нові запуски; після перевірки відновлено початковий стан (рядок відсутній).
- Резервна копія бази: `/opt/thesica/backups/pre-deploy-20260909-161454.sql`, 26 168 848 байтів, завершення pg_dump підтверджено. SHA-256: `d3127ee11f71a00e1d0f1e46889bc1e054138a61077d2d459f885736fab50d4e`.
- Резервна копія попереднього коду: `/opt/thesica/backups/executor-v2-before-6e432a4/source-6ff8c5c.tar.gz`. Попередні образи API/web позначено `rollback-20260909-161454`.
- На сервері 513/513 файлів відповідають погодженому коміту; у контейнері 138/138 Python-файлів відповідають йому.
- API, web і бот healthy; перезапусків після підняття — 0. Бот зберіг попередній образ і час запуску. Compose-конфігурація не змінилася.
- Встановлення остаточно зафіксоване о 16:21:02 UTC. Після цього код, образи, конфігурація й історія старих спроб не змінювалися.

## Чому контроль не почався

Власник увійшов у кабінет як manager1. Сторінку `/dashboard/documents/11` відкрито й перезавантажено після встановлення. Робота №11 має `status=completed`; остання спроба — legacy job12, завершена до цього встановлення.

Кабінет у `apps/web/app/dashboard/documents/[id]/page.tsx:261` показує `TaskContractPanel` лише для порожньої чернетки або `failed/failed_quality/cancelled`. Для завершеної №11 панелі та кнопки нового запуску немає. Спостережений список дій: «Назад», «Завантажити DOCX», «Надіслати фідбек» (плюс навігація).

Водночас штатний backend recovery для job12 повертає `allowed_actions=["new_version"]`. Отже, нова спроба підтримана API, але недоступна через кабінет. Це перешкоджає саме погодженому наскрізному маршруту.

## Платний прогін та критерії

- Нових спроб генерації: **0**. Натискань платного запуску: **0**.
- Витрати нового прогону №11: **$0** (прогін не почався). Це не облік окремого рецензування Fable.
- Нового DOCX, запису моделі та точного replay немає; перевірку посилань і відсутності заглушок нового документа виконати неможливо.
- Попередній DOCX job12 не видається за результат executor v2.
- API напряму не викликався для запуску, статус документа вручну не змінювався, жодних виправлень чи повторних встановлень не виконувалося.

## Рішення Fable та наступний крок

Fable отримав докази встановлення, стан №11, спостереження кабінету й відповідний код. Вердикт: це реальний блокер саме погодженого контролю; встановлення підтверджене, приймання прогону відсутнє. Фриз зберегти.

**Рекомендація Fable:** створити через кабінет нову чернетку з тим самим завданням №11 та провести єдиний контроль на ній. Встановлена версія залишається без змін, повторний деплой не потрібний. Потрібен окремий дозвіл власника лише на інший номер документа.

Для точного перенесення збережено [вхідні дані й контракт №11](document11-brief-copy.json) і [повний текст вимог](document11-requirements.txt). Завантажених джерел немає. Методичка позначена як опрацьована; її повний текст збережено всередині вимог і окремо у [TXT](document11-methodology-extracted.txt). Шляху до початкового файла в документі немає, тому тотожність початкового завантаженого файла не стверджується. Перед будь-яким стартом у новій чернетці потрібно звірити всі поля, повний текст вимог, наявність методички та SHA контракту з цим записом. Нову чернетку поки не створено.

Якщо власник наполягає саме на №11, альтернативою є окремо погоджена зміна інтерфейсу, перевірка Fable та нове встановлення. Жодну з альтернатив ще не виконано.

## Докази

- [Штатний журнал встановлення](install.log)
- [Перевірений стан перед прогоном](server-accepted-before-run.json)
- [Відсутність генерації та змін](no-generation-or-drift.json)
- [Стан №11 та дозволена API дія](cabinet-blocker-server.json)
- [Спостереження кабінету](cabinet-observation.json)
- [Запит до Fable з доказами й кодом](fable-prompt.txt)

- [Висновок Fable](FABLE.md)
- [Повна відповідь Fable](fable-response.json)

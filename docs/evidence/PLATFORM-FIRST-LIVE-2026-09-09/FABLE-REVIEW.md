**DIRECTION_APPROVED.** Прогін відповідає доручению: незмінний №11, один POST у штатному кабінеті, лише попередження, суворість на видачі. Критерій цього контролю — DOCX — **досягнуто**: job12 завершився з першої спроби, 6 розділів, файл 50995 байт, SHA `cad0dff0…`, той самий файл штатно завантажений (`ui-download-proof.json`). Це не приймання якості, трьох замовлень чи Танею: release_status=blocked, delivery not_ready, Compilatio не виконувався.

**Реальні докази поведінки попереджень.** Спрацювали саме ті перевірки, які раніше зупиняли письмо, і не зупинили: рецензент плану `source_coverage_gap` → warning (id 515); grounding «no grounded citation in section» у розділі 5 (той самий стоп, що вбив doc 53) → warning; claim-перевірки «невизначені» → warning; GPTZero 90–100% failed, non-blocking; Copyscape «Insufficient credit» → unchecked; підсумковий academic review failed → лише гейт видачі. Обрізаний план (002, `max_tokens`, 4000 out) і штатний повтор (003, `end_turn`) обидва збережені.

**Запис і replay.** Повнота: 88 started/received для 44 викликів, INDEX 0 failed / 0 unfinished, ключів і `lease_token` у FULL-RECORDING немає (0 збігів). 12 збігів `failed/outcome_unknown` — це старі події job11 та «pending»-квитанції етапів підготовки/рецензії job12, не прогалини провайдера. Replay: stock-CLI 44/44 запити незмінні, 0 живих викликів, DOCX ідентичний по `word/document.xml`, різниця лише в `docProps/core.xml`. Але stock-CLI дає `profile_changed=true`: `PARTIAL_COMPLETION_ENABLED` не входить у `REPLAY_SETTING_NAMES` (префікса `PARTIAL_` немає), точний профіль отримано лише операційним wrapper. Висновок: збережені відповіді повні; точний replay доведений, але штатна команда сьогодні його не дає без обгортки.

**Витрати, незмінність, історія.** $3.36 збігається з таблицею тарифів (Opus 4.8 $5/$25, панель gpt-4.1-mini): моя оцінка ≈$3.37; це облік застосунку, не рахунок. Невідомих витрат 0. Сервер/конфіг/образи незмінні (134 файли, хеші конфігів, одна платна заявка), job11 і 17 його подій збережені.

**Знахідки за пріоритетом.**

1. **Мітки перевірки в прозі DOCX (платформа, prompt).** 14 фраз «da verificare» / «soggette a verifica» у тексті (`DOCX-TEXT.txt:13–64`). `STANDARD_SOURCE_RULES` забороняє мітки в прозі, але `academic_context.evidence_rule` у warning-режимі каже «require manager verification» і подається моделі як дані — вона це переказує. За правилом no-rewrite людина цього не виправить, отже фікс лише на боці prompt: прибрати «manager verification» з тексту, що бачить модель; перевірка — справа системи. Перевірити повторним викликом одного етапу, без повного прогону.
2. **Гейт цитат не бачить стандартних джерел (видача, PF06).** `citation_verification` = passed, `verified: 10`, тоді як бібліографія DOCX містить 7 неперевірених: McKinney, NANDA-I, NOC, WHO×2, D.M. 739/740. За новим рішенням фаундера такий файл не видається; гейт має рахувати стандартні посилання, а напис у кабінеті «потребують перевірки менеджером» суперечить правилу.
3. **Неточність звіту.** «30 записів: 24 verified, 6 стандартних unverified» — це таблиця джерел документа (24 verified + 6 відхилених кандидатів Kumaran/Redshaw/Cadei/Balzano/Schmalfuss/Parricchi, жодного з них у DOCX немає). Фактична бібліографія DOCX: 17 записів = 10 verified із пакета + 7 unverified стандартних.
4. **Allowlist replay.** Вивести `REPLAY_SETTING_NAMES` із `PROFILE_SETTINGS` (або додати `PARTIAL_`), з регресією на цьому записі, щоб stock-CLI давав `profile_changed=false`.
5. **Рендер інституційних авторів (косметика, у PF06).** «(Sanità, 1994)» і «Sanità, M. D. (1994)» замість Ministero della Sanità; 5 повних імен у дужках для McKinney; «(World Health Organization)» без року (рядки 60, 64). Стандартна бібліотека з фіксованими метаданими закриє це разом.
6. **mypy.** 46 доданих — анотації; два `arg-type` (`AdvisoryConfig | Settings`, `Exception | RecordingPersistenceError`) runtime-безпечні за задумом, але типізувати варто. CI не зелений, так і казати.
7. **Калібрувальні приклади з запису, не стопи:** рецензент пише «менше 4500 слів» при 5902; GPTZero 90–100% на всіх розділах — сигнал для планки 10/10, не для цього критерію.

**Межі приймання.** Одне замовлення, не три; Compilatio/no-rewrite не виконувалися; DOCX із 7 неперевіреними посиланнями та мітками в прозі не є файлом на видачу.

**Наступний крок.** PF06 у поточній черзі: системна перевірка існування/метаданих/змісту книг і guidelines, стандартна бібліотека NANDA-I/NOC/NIC/WHO/італійських підручників, гейт цитат з урахуванням стандартних посилань, прибрати мітки перевірки з prompt (п. 1) і allowlist replay (п. 4). Нових платних прогонів для діагностики не потрібно: цей запис відтворюється за $0.

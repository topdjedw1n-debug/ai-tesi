# Перебудова виконавця — крок 0, 09.09.2026

Крок 0 виконаний локально: канон у Git, робочі гілки включені в `main`,
код `apps/` та `infra/` дорівнює фактично встановленому `6ff8c5c`.
Наступна дія — очікувати контракт від Fable за
[єдиним чинним планом](../plans/THESICA-REBUILD-EXECUTOR-2026-09-09.md).
Відправлення `main` і збіг віддаленої вершини перевіряються після коміту
цього запису; фінальний SHA повідомляється у звіті виконавця.

## Git і канон

- `caa78e5`: AGENT_SYNC, PRE-RUN, усі наявні docs/plans і docs/evidence,
  включно з повним записом №11. Погоджений план збережений без зміни.
- У AGENT_SYNC, PRE-RUN та індексі plans явно визначено один чинний план;
  старі черги та результати збережені як історія. Кроки 1–6 не починалися.
- `5395695`: злито готове видалення старого AWS-autodeploy. Це зміна
  автоматизації репозиторію; вона не змінює жодної серверної служби.
- `c734160`: історії решти гілок зведені. Усі 34 перевірені локальні та
  віддалені refs входять у `main` за ancestry; гілки не видалені.
- Для старих `claude/gifted-goodall-bd8f0a`,
  `claude/gracious-sinoussi-27157a`, `claude/keen-villani-14a28c` використано
  merge `ours`: історія збережена, їхні невстановлені правки не внесені в
  кінцевий код. Це потрібно для умови `main` = код сервера.
- `codex/provider-balance-recovery` також зведено через `ours`: потрібні
  документи й докази вже включено до свіжого канону; старі зміни роадмапу
  не повернуто як поточні інструкції. Решта гілок уже входила в лінію
  встановленого `6ff8c5c`.

## Перевірка рівності

- Усі **503** відстежувані файли `apps/` і `infra/` у Git побайтово
  збігаються з `/opt/thesica`; порівнювалися SHA-256 вмісту.
- Усередині запущеного API окремо звірено **134** файли; у боті — **3**.
  Збірка web `J3UqeojCFGJ1GP82jsmjH` і образ збігаються з установленим
  релізом. Ідентифікатори образів наведені в [JSON-доказі](REBUILD-STEP0-2026-09-09.json).
- `git diff 6ff8c5c -- apps infra` порожній. Нові коміти додають документи,
  історію злиття та видаляють застарілий workflow; SHA `main` закономірно
  відрізняється від серверного SHA, код застосунку — ні.
- Повторне читання сервера підтвердило незмінні файли, контейнери, образи,
  час запуску та кількість перезапусків. Усі три прикладні служби healthy.
- Перевірка JSON та пошук явних ключів доступу у новому каноні, включно з
  вмістом ZIP, пройшли. Архіви, записи та погоджений план збережені у
  вихідних байтах; для цього не застосовувалися автоматичне форматування
  історичних доказів і стандартне обмеження 1 MB на доказовий ZIP.
- Код не змінювався, тести генерації/replay не запускалися. Фінальний
  коміт лише з документацією позначено `[skip ci]`, щоб автоматичний CI
  не запускав старого виконавця у тестах цього кроку.

## Межі та продовження

Старого виконавця не запускали; платних звернень — 0; встановлень,
перезапусків, записів у БД або змін файлів сервера немає. Це завершення
кроку 0, не приймання нового виконавця чи академічної якості.

Основна папка зберегла всі сторонні незакомічені файли й попередньо
підготовлені до коміту перейменування. Для `main` використано окрему
чисту робочу копію:
`/Users/maxmaxvel/AI TESI/.scratch/rebuild-step0-20260909/main`.
У наступній сесії коду використовувати її або нову копію від `main`;
основна папка залишається на своїй старій гілці з незавершеними правками.

Стан наступного кроку: **WAITING_FOR_FABLE_CONTRACT**. Контракт не
замовлявся і не реалізовувався в цьому дорученні.

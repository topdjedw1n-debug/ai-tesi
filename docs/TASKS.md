# TASKS — відкриті задачі Thesica

> Живий список задач. Оновлено 04.07.2026. Контекст рішень:
> [phase1-runs/HANDOFF-2026-07-03.md](phase1-runs/HANDOFF-2026-07-03.md).
> Стратегія: [../THESICA-PLAN.md](../THESICA-PLAN.md).

## 🔴 Негайні — чекають ручної дії фаундера

- [x] **T1. Compilatio для Блоку 1** — **виконано фаундером 04.07.2026.**
  Результат (підозріле всього / з них AI / схожість):
  **Opus 20%/16%/5%** · Sonnet-5 44%/38%/8% · gpt-4o 80%/76%/6%.
  Висновки і наслідки — HANDOFF §4 (Блок 1). Головне: сирий Opus уже в
  смузі рятованого (14–21%) → рятувальник, імовірно, не потрібен;
  Блок 2 під питанням (див. T2).
- [ ] **T1b. Compilatio для Блоку 1b (gpt-5.5, gpt-5.4).** Прогнати ще 2
  файли `~/Downloads/THESICA-BLOCK1/{gpt55,gpt54}/block1-<tag>.docx`
  (та сама тема, сирий текст; згенеровано 04.07 за «так» фаундера,
  $0.52+$0.32) і вписати цифри в таблицю Блоку 1.

## 🟡 Експеримент детектор-у-циклі (платне, потрібне «так» фаундера)

- [ ] **T2. Блок 2 — ПІД ПИТАННЯМ після Блоку 1.** Сирий Opus = 16% AI
  (у смузі рятованого), тож повна матриця рятувальників може бути зайвою.
  Дешевша альтернатива: **перевірка генералізації** — сирий Opus на Темі 2
  (регуляція криптовалют в ЄС), 1 генерація ~$1 → Compilatio. Якщо знову
  ≤20% — рятувальник вимикаємо; якщо вище — повертаємось до матриці (~$4).
- [ ] **T3. Блок 3 — флагман (опційно).** Актуальний лише якщо рятувальник
  все-таки потрібен.
- [ ] **T4. Зафіксувати фінал.** За результатами T2 — або
  `HUMANIZER_ENABLED=false` у прод (мінус вартість rescue+judge на кожну
  роботу), або остаточний `HUMANIZER_MODEL`. Окремо: перевпорядкувати
  `AI_FALLBACK_CHAIN` (Sonnet-5 поперед gpt-4o — HANDOFF §4 висновок 5).

## 🟢 Технічний борг (безкоштовно, знайдено 03.07.2026)

- [x] **T5. Прайс-таблиця вартості неповна.** ~~«Облік занижує»~~ —
  насправді `UsageTracker` вже рахував кожен виклик за ціною його моделі,
  але нових дешевих моделей у прайсі не було → невідомі білились за
  бекстопом gpt-4 ($30/$60), собівартість ЗАВИЩУВАЛАСЬ. **Виконано
  04.07:** додано gpt-4o/-mini, gpt-4.1/-mini/-nano, gpt-5.4/-mini/-nano
  (звірено з офіційним прайсом OpenAI); бекстоп збережено.
- [x] **T6. Звести незакомічені зміни** — **виконано 04.07**, робоче
  дерево обох сесій розкладено в логічні коміти (git log 04.07).
- [ ] **T7. Ротувати ключ OpenRouter** — світився в чатах (`apps/api/.env`).

## ✅ Зроблено 04.07.2026

- **gpt-4-дефолти викорінено остаточно:** консольна форма не шле модель, і
  каскад дефолтів (schema `DocumentCreate` / validator / endpoint /
  `document_service` / ORM-колонка / error-fallback ланцюга) писав би
  кожен документ менеджера старим gpt-4 ($30/$60, Compilatio 53%). Усі 7
  точок → `anthropic/claude-opus-4-8` (валідатор provider-aware: явний
  openai без моделі → gpt-4o). Доведено POST-ом без моделі (у БД
  anthropic/claude-opus-4-8) + 747 тестів зелені.
- Артефакт gpt-4o Блоку 1 перезібрано (див. T1).
- Прайси нових моделей у `cost_estimator.py` (див. T5).

## ✅ Зроблено 03.07.2026 (для контексту)

- Фаза 4/4b/4c/5: best-of-N + заморозка цитат; Opus-писар доведено на
  Compilatio; gpt-4 викинуто; суддя відв'язаний від ланцюга.
- Фікси: length-fair grammar-гейт, target_word_count у промпт, corruption
  guard, честий слід писаря, мертвий download_file, session_token-колізія,
  ізоляція тестів від .env, стійкий JSON-розбір (gpt-4o markdown-огорожа),
  оновлений allowlist моделей.
- Блок 0 (суддя): gpt-4.1-mini обрано (поріг 60), gpt-5.4-nano відкинуто.
- Блок 1 (писарі): Opus/Sonnet-5/gpt-4o написали сирий текст → чекає T1.
- Промпт писаря дотягнуто під 5 human-markers; гуманізатор-промпт +
  stylometrics вимірювач (паралельна сесія).
